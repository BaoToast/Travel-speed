# 交通服務水準分析系統 v2.20.4 驗證紀錄

日期：2026-08-26

## 本版範圍

- 保留 v2.20 已確認的計算規則與功能。
- 更新 Excel 讀取元件至官方 SheetJS 0.20.3。
- 補齊匿名、可重現且不含正式資料的測試範例。
- 修正交付包在其他電腦無法獨立執行測試的問題。

## 驗證結果

- 單元測試：25/25 通過。
- 瀏覽器操作測試：15 個主要畫面通過，涵蓋 640、1024、1280、1440、1920 像素寬度。
- 舊版 Excel：`.xls` 尖峰資料辨識成功；`.xlsx` 辨識成功。
- 正式調查檔抽樣：5/5 與原檔彙總表一致，0 筆差異。
- 代表紀錄延滯固定案例：旅行速率 20.528、行駛速率 34.732、同筆總延滯 132 秒，測試通過。
- 套件安全檢查：0 個已知弱點。

## 發布限制與資料保存

- 本系統是純瀏覽器網站；使用者匯入的正式資料保存在該瀏覽器本機，不會隨網站程式發布。
- GitHub Pages 與 GPT Site 的程式功能相同，但不同電腦或不同瀏覽器不會自動共享資料。
- 發布包不含正式調查檔、Project 備份或個人資料。
- 本專案的來源包不需要且不包含 `.openai/hosting.json`，以免改變既有 GPT Site 專案識別。

## v2.20.2 追加驗證（2026-08-24）

本版沒有變更任何計算規則、判定門檻或操作流程，v2.20.1 的驗證結果全部沿用；
以下是本版新增項目的驗證：

- 單元測試：**29/29 通過**（v2.20.1 的 25 項 ＋ `xlsx-hardening.test.mjs` 4 項）。
- 版本一致性檢查（`check-version.mjs`）：14 項全部通過，全站只有 `app.js`
  一處寫入版本字樣，`index.html` 的 `?v=` 參數與手冊檔名皆為 2.20.2，
  沒有殘留其他版號。
- 瀏覽器操作測試：`npm run e2e` 三支腳本全部通過，**120 項 ✅、0 項 ❌**，
  涵蓋 640～1920 像素共 9 種寬度。
- 原型污染防護：以測試在解析前後注入一個屬性到 `Object.prototype`，
  確認會被偵測、被刪除、並丟出帶檔名的錯誤而中止匯入；未被污染時不做任何事。

### 本環境未能驗證的一項（請由具備 CDN 連線的環境確認）

`npm ci` 需要抓取 `https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz`，
而製作本版的環境對該網域回 **403（安全政策封鎖）**，因此無法在此重跑
`npm ci`。上述單元測試與瀏覽器測試是在既有安裝環境下執行的：
**瀏覽器端讀的是包內 `vendor/xlsx.full.min.js`，已確認為 0.20.3**；
Node 端產生測資時解析到的是外層既有的 0.18.5。
`npm ci` 本身已由上一版的製作方在可連線的環境驗證通過。

## v2.20.3 追加驗證（2026-08-25）

本版沒有變更任何計算規則、判定門檻或操作流程，v2.20.2 的驗證結果全部沿用。
本版修正的是備份範圍：結論草稿的「條件範本」原本不在專案包與個人全部計畫包內，
換一台電腦匯入後會全部消失。已補進匯出與匯入兩端。

- 新增 `backup-completeness.test.mjs`（4 項）：清單比對匯出／匯入兩端是否收齊
  使用者自訂設定；並釘住「備份檔沒有該欄位時不得清掉本機既有範本」。
- 既有測試全部重跑，未出現迴歸。

## v2.20.4 追加驗證（2026-08-26）

本版沒有變更任何計算規則、判定門檻或操作流程，v2.20.3 的驗證結果全部沿用。
本版修正的是手冊的版號與日期來源。

- v2.20.3 的手冊封面戳記為 `2026-08-25`、頁尾為 `2026-08-24`，兩處不一致。
- 新增 `manual-src/release.mjs`，頁尾字樣與手冊檔名一律由封面戳記推導。
- `check-version.mjs` 由 14 項增為 18 項，新增的四項為：
  找得到封面戳記、戳記版號等於 `app.js` 的版號、
  `build-pdf.mjs` 與 `build-docx.mjs` 沒有寫死的版號或日期。
- **已實測新檢查會失敗**：把寫死的日期塞回 `build-pdf.mjs` → 該項紅字；
  把封面戳記改成 v2.20.9 → 版號比對項紅字；還原後全部通過。
- 既有測試全部重跑，未出現迴歸。
