# 交通服務水準 v2.20.31 驗證報告

- 驗證日期：2026-09-04
- 前一正式版：v2.20.28（commit `c581e58beac1b02110984c494c5b442d3cc7efa5`）
- 發布目標：`BaoToast/Travel-speed` GitHub Pages

## 複查結論

本版以 Claude 交付的 v2.20.30 為基礎複查。季度輸入與路段有效期間修正可通過回歸與瀏覽器流程；另發現並修正三項交付可靠性問題：

1. 新手手冊精簡時漏掉路段有效期間、年份／期別顯示及西元／民國季度輸入的現行操作說明。
2. 手冊指向不存在的 `CHANGELOG.md`。
3. 測試端 SheetJS 仍依賴外部套件網址，乾淨環境可能因該站無法連線而無法重建；手冊副本測試又把 v2.20.30 寫死，下一次升版容易漏檢。

本版補齊教學、移除無效引用；將經核對的官方 SheetJS 0.20.3 安裝包放入 `vendor/`，以本機相對路徑鎖定，並新增 SHA-256 守門檢查；手冊副本測試改由手冊的單一版本來源推導檔名，且要求 PDF、DOCX 兩種格式都存在。

## 功能與計算邊界

沒有變更旅行速率、行駛速率、延滯解析或加總、LOS 門檻、代表紀錄選取、各計畫門檻隔離、備份還原或 Manager 比較的計算規則。

- LOS：A ≥ 0.80、B ≥ 0.60、C ≥ 0.50、D ≥ 0.40、E ≥ 0.20、F < 0.20。
- 代表紀錄：先取最差 LOS，再取最低速限比，再取最低旅行速率。
- 旅行速率、行駛速率與總延滯由同一筆代表紀錄帶入。

## 實際驗證

- 乾淨安裝：內附 SheetJS 0.20.3 安裝包可由 `npm ci` 安裝；檔案 SHA-256 為 `8DC73FC3B00203E72D176E85B50938627C7B086E607C682E8D3C22C02BB99FE8`。
- Node 單元／回歸：121 項，119 通過、0 失敗、2 略過。
- 略過原因：本次附件沒有真實交通調查 Excel，兩項真實工作簿檢查按設計略過；不把它們宣稱為已驗證。
- 瀏覽器端對端：8 支腳本執行完成，共 257 項綠色檢查、0 JavaScript 例外。
- 版號與檔案：畫面、快取參數、README、手冊檔名、手冊封面與本報告一致。
- 手冊：PDF 20 頁、Word 135 個內容區塊；PDF 全頁及 Word 轉 PDF 的 16 頁均轉圖檢查，未見裁切、重疊或缺字。
- 交付限制：沒有 `.openai/hosting.json`、`node_modules`、個人計畫資料或 GPT Site 交接檔。

## 證據界線

Claude 說明所列的真實調查工作簿跨系統驗證，因這次交付包沒有附原始工作簿，無法在本次環境獨立重做。以上結論只涵蓋本次實際執行的匿名測資、靜態守門、乾淨安裝、瀏覽器流程與文件視覺檢查。
