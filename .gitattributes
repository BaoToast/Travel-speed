# 換行字元不要自動改寫。
#
# 在 Windows 上取出時，Git 預設（core.autocrlf=true）會把文字檔轉成 CRLF。
# 姊妹系統實測過後果：備份 ZIP 裡每一個文字檔都變成 CRLF，連建置產物也一樣，
# 於是 assets/index-<雜湊>.js 的內容不再雜湊成它自己的檔名，
# 備份包與線上內容無法逐位元核對——而網站照樣跑得動，完全看不出來。
#
# 本專案的交付包裡實測有 19 個文字檔是 CRLF，成因相同。
* -text
