# 交通服務水準 v2.20.46 驗證報告

- 驗證日期：2026-09-08
- 前一正式版：v2.20.43（GPT 發布，commit `9dd9c8d97b3552d2a4858484007707dc1661558a`）
- 交付基準：Claude 提供的 v2.20.45
- 風險等級：**中風險**（結論草稿格式設定跨三條文字輸出路徑；另追查前版資料庫錯誤分流）
- 本版性質：**修正資料庫錯誤分類與結論草稿位數邊界；交通計算完全沒有變更。**

## v2.20.46 風險導向複查發現與修正

### 一、較高版本資料庫被誤判為瀏覽器封鎖

v2.20.44～v2.20.45 固定呼叫 `indexedDB.open(DB, 1)`，並把任何非同步開啟錯誤
都設成 `storageBlocked = true`。若同名資料庫已是版本 2 以上，瀏覽器會正常回傳
`VersionError`；這不是權限封鎖，但畫面會錯誤宣稱瀏覽器不允許儲存，且原計畫無法載入。

**修正**：開啟時不指定較低版本，沿用現有資料庫版本；只把 `SecurityError`、
`NotAllowedError`、`InvalidStateError` 或 IndexedDB 根本不可用等明確情境判定為封鎖。
其他錯誤仍走原本的資料搶救流程。新增版本 2 資料庫案例，原 v2.20.45 有 2 項失敗，
修正後可載入代碼 `HIGH` 的原計畫。

### 二、舊條件範本的位數缺漏會無聲改成 0 位

Claude v2.20.45 正確把百分比改為 `pct(value, digits)`，但註解所稱
`toFixed(undefined)` 會拋錯並不符合 JavaScript 行為；實際結果是默默輸出 0 位。
此外，匯入條件若超出 `toFixed` 可接受範圍，會拋出 `RangeError` 並中斷草稿。

**修正**：`buildConclusion()` 在組字前統一驗證 `digits`，只接受畫面提供的 0、1、2；
缺漏、空值、非數字或超出範圍時安全回到既有預設 1 位。`pct()` 本身仍維持必填參數，
不改回會掩蓋漏傳的函式預設值。新增 5 種異常輸入的守門案例；原 v2.20.45 會失敗，
修正後旅行速率與所有百分比都一致輸出 1 位。

### 三、文件證據補正

Claude 的 v2.20.45 驗證報告只有改檔名與標題，內文沒有記錄該版百分比修正，且仍以
v2.20.44 的三項修正為「本版」。本報告已補回 v2.20.45 的原始修改、上述獨立發現與
測試邊界。Claude 說明中的「125 項全綠」精確結果應表述為 125 項中 123 通過、2 略過；
略過的是交付包未附真實工作簿時的條件式測試，不是失敗。

## v2.20.45 原始修改（Claude）

- 結論草稿的速限比、LOS 等級筆數比例及季度變動比例，均跟隨使用者選擇的 0～2 位。
- 新增 `conclusion-digits.test.mjs`，原始兩項守門均能抓到舊版寫死一位的問題。
- 速率、延滯、LOS 門檻與代表紀錄挑選均未改動。

## v2.20.44 沿用的三項修正

### 一、儲存空間被瀏覽器封鎖時，搶救畫面說了兩句不成立的話

把 `window.indexedDB` 改成「存取即拋錯」（瀏覽器設定成「封鎖所有 Cookie／
網站資料」時的實際行為）後開啟系統，v2.20.43 顯示：

> 儲存在瀏覽器裡的資料有一部分格式不符…**您的原始資料仍然完整保留在瀏覽器裡。**

兩句都不成立：資料沒有損壞，這台電腦上根本沒有本系統的資料。接著按
「下載原始資料備份」→ 跳「備份下載失敗：IndexedDB 已被停用」。

**處理**：`openDB()` 把 `indexedDB.open` 包進 try（存取那個屬性本身就會丟
SecurityError），並記下 `storageBlocked`；`showLoadError()` 依此分成兩種畫面。
封鎖版明講原因、列出三種常見設定與處理方式、給一顆「調整設定後，重新載入」，
**不再提供那顆一定會失敗的下載鈕**，並提醒此時不要匯入。

### 二、搶救畫面出現時，底下有一則未捕捉的例外

`showLoadError()` 會把整個 `.app` 換掉，但 `index.html` 在 `app.js` 之後還載入
`conclusion.js` 與 `quality-extension.js`，那兩支在頂層就會找主畫面裡的節點
（例如 `q("roadAlert").after(importPanel)`）。換得太早就會丟
「Cannot read properties of null (reading 'after')」。

**處理**：`readyState` 還在 `loading` 時掛一次 `DOMContentLoaded` 再換。

> 註：這一項只在「儲存空間被封鎖」那條路會踩到（`openDB` 同步丟例外，catch
> 立刻執行）；「資料壞掉」那條路要先 reload 再等 IndexedDB 非同步讀完，那時
> 腳本早就跑完了。所以 `e2e-load-failure.mjs` 裡新增的那一項在 v2.20.43 就是
> 綠的，它是**不許改壞的鎖**，不是問題重現——已在該檔註解明寫。

### 三、文件宣稱了一個本系統沒有的功能

README 與驗證報告從 v2.20.41 起都寫著「『混合時間格』仍只顯示警告」。
整份程式搜過，**沒有任何地方產生這個提醒**：健康檢查共八類（異常名稱、
名稱疑似重複、方向對應不一致、資料組不完整、日別不完整、數值異常、
異常變化、速限未確認），不含此項。那是全日交通量與路口轉向才有的檢查——
它們讀逐時距計數，必須判斷「一格幾分鐘」；本系統讀的是報告裡已經算好的
尖峰旅行速率與延滯，沒有時間格長度的概念。

**處理**：刪除該句，並在 `dependency-manifest.test.mjs` 加一項守門：
程式沒有實作就不准寫回文件；真的做出來了會自動放行，不會擋住未來的功能。

## 突變測試

| 對程式做的破壞 | 結果 |
|---|---|
| 換回 v2.20.43 的 `app.js`＋`styles.css`，跑 `e2e-storage-blocked.mjs` | ❌ **8 項紅字**（畫面說謊、下載鈕還在、沒有出路、未捕捉例外…） |
| 把「混合時間格」那句寫回 README | ❌ 文件守門紅字 |
| 移除後 | ✅ 全過 |

`e2e-storage-blocked.mjs` 同時驗「儲存空間正常時不可以誤跳封鎖畫面」，
避免寫成無條件顯示也會通過。

## 計算未變更的證明

`period-date.js`、`column-filter.js`、`excel-export.js`、`quality-extension.js`、
`los-and-selection-golden.test.mjs` 與正式 v2.20.43 **逐位元相同**。
`conclusion.js` 只有 Claude v2.20.45 的百分比顯示位數修正，以及 GPT v2.20.46
在組字前驗證 `digits` 的防呆；沒有改 LOS 門檻、代表紀錄挑選、旅行速率、
行駛速率或延滯數值。`app.js` 的變更限於資料庫開啟與搶救畫面，未改解析及計算路徑。

## v2.20.46 最終驗證結果

- 單元／回歸測試：126 項，其中 **124 通過、0 失敗、2 略過**。
- 略過的兩項只在交付包附真實工作簿時執行；匿名測資、程式產生測資及所有永久守門均已執行。
- 版號一致性檢查：全部通過。
- 端到端瀏覽器驗證：**16 支腳本全部通過**；包含較高版本 IndexedDB、儲存封鎖、
  讀取搶救、匯入解析、代表紀錄、LOS、結論位數、圖表、CSV／Excel 匯出及重複匯入。
- 輸入 → 解析 → 計算 → 顯示 → 匯出：測資的 4 筆來源值、最差代表紀錄、LOS 與匯出值一致。
- 黃金計算案例：7 項全部通過。
- TypeScript／lint／production build：本專案是直接發布的靜態 JavaScript，沒有 TypeScript、
  lint 或 build 指令，故不適用；沒有以「略過」冒充成功。
- PDF 手冊：21 頁逐頁渲染檢查通過。
- Word 手冊：以 Microsoft Word 2024 實際開啟並輸出為 18 頁，逐頁渲染檢查通過；
  未發現截字、重疊、缺字或表格斷裂。

## 發布證據

本節在 GitHub Pages 發布後補入正式 commit、GitHub Actions 與公開網址核對結果。

---

以下保留 v2.20.43 的原始驗證內容。

- 驗證日期：2026-09-06
- 前一正式版：v2.20.41（GitHub commit `8b99601`）
- 交付基準：Claude 提供的 v2.20.42
- 本版性質：**延續讀取失敗保護，補上損壞根資料可能被空白預設值掩蓋的缺口。計算完全沒有變更。**

## v2.20.43：損壞根資料必須進入搶救模式

### 獨立複查發現

v2.20.42 能攔截資料遷移或 `rebuild()` 丟出的錯誤，但讀取成功後會直接把資料與
`emptyState()` 合併。若 IndexedDB 內是 `{ unexpected: "仍是原始內容" }` 這類可讀取、
卻完全不含計畫結構的物件，合併後仍可通過 `rebuild()`，畫面會被誤顯示成空白系統，
未進入搶救模式。之後若使用者建立計畫，就可能覆寫原始內容。

### 修正

- 只有 IndexedDB 根值為 `undefined` 時才視為首次使用並建立空白狀態。
- 既有根值必須是目前的 `projects` 多計畫格式，或可移轉的舊版 `project` 單計畫格式；
  其餘格式在合併預設值之前立即丟出錯誤，沿用 v2.20.42 的鎖存檔與搶救畫面。
- 保留舊版單計畫格式的正常移轉路徑，不把合法舊資料誤判為損壞。

### 強化後端到端實測

| 檢查 | 結果 |
|---|---|
| 讀取失敗後顯示搶救畫面 | ✅ |
| 搶救狀態下呼叫 `save()` 不改寫資料庫 | ✅ |
| 實際按下下載按鈕並解析 JSON，保留原計畫與損壞紀錄 | ✅ |
| 根資料完全不含計畫結構時進入搶救模式 | ✅ |
| 舊版單計畫格式仍可正常移轉 | ✅ |

針對第四項把根格式檢查拿掉時，測試會明確失敗，證明守門測試能捕捉本次缺陷。


## v2.20.42：讀取失敗時不再靜靜清空（最後整體徹查發現）

### 問題

`load()` 的 `catch` 是 `state = emptyState()` 然後照常 `renderAll()`。
IndexedDB 讀取失敗、或儲存的資料裡有任何一筆讓 `migrateLosRules()` /
`rebuild()` 丟例外時，整個 state 被換成空的——**畫面上沒有任何訊息**：
沒有 toast、沒有頁面錯誤、沒有搶救畫面，只顯示「尚未建立計畫」。

使用者以為資料沒了，於是重建計畫或重新匯入，那個動作呼叫 `save()`，
把空白 state 寫回 IndexedDB。**原始資料這時候才真的消失，而且救不回來。**

路口轉向早已修過同一個缺陷（`loadError` ＋ 整頁搶救指引，
`app/traffic-app.tsx:3814` 的註解逐字寫著同樣的推理）。這一支沒跟上，
也沒有任何測試涵蓋這條路徑。

### 實測重現（修正前）

種入一筆會讓 `rebuild()` 丟例外的壞紀錄：

```
種入前 DB：           {"projects":["REAL"]}
重新載入後：           計畫選單 []｜toast「」｜搶救指引: false｜頁面錯誤：（無）
此時 DB 仍是：         {"projects":["REAL"],"details":1}   ← 資料還在
按一次「儲存計畫設定」後：{"projects":["NEW"],"details":0}    ← 原始計畫消失
```

### 修正

比照路口轉向：讀取失敗時記下 `loadError`、**鎖住 `save()`**、把整個畫面
換成搶救指引，並提供「下載原始資料備份」按鈕（直接從 IndexedDB 取原始
blob 存成 JSON）。主畫面不再 render，因為主畫面一旦 render 就會開始存檔，
那才是真正把資料弄丟的那一步。

### 實測（修正後）

```
重新載入後：           搶救指引: true｜主畫面表單進不去
此時 DB 仍是：         {"projects":["REAL"],"details":1}
嘗試呼叫 save() 之後：  {"projects":["REAL"],"details":1}   ← 原始資料保住
```

### 突變測試

新增 `e2e-load-failure.mjs`。把 `catch` 改回舊的靜靜清空、並移除存檔鎖：
**4 項紅字**，其中最關鍵的「讀取失敗之後任何存檔都不可以覆蓋掉原始資料」
量到「計畫（無）、明細 0 筆」。

**沒有變更任何計算、判定門檻或資料格式。** 只動錯誤處理路徑與一段搶救畫面樣式。

## v2.20.41 獨立複查修正

### 一、連續開啟選檔視窗時，上一輪計時器會提前收掉提示

v2.20.40 已處理「連按兩次再取消會永久卡住」；加強操作時序後仍發現：
第一次取消排出的 1.2 秒計時器，可能在第二次選檔視窗仍開啟時觸發，
把「正在讀取」提前改回「尚未選取檔案」。新一輪選檔開始時現在會先清除
上一輪計時器。永久測試會等待舊計時器原本應觸發的時間，再確認提示仍存在。

### 二、預覽用 `parsedLimit` 被帶進正式資料

v2.20.40 為修正合併／別名路段的預覽 LOS，將解析當下速限暫存在列物件的
`parsedLimit`。正式寫入沿用同一個列物件，因此該預覽專用欄位也會進入
尖峰明細與備份。現在改以 `WeakMap` 保存預覽暫存值，不改正式資料結構；
永久測試會在確認寫入後直接檢查正式明細不存在該欄位。

兩項均已先在未修正的 v2.20.40 上證實測試會失敗，再套用修正。
LOS 門檻、代表紀錄、旅行速率、行駛速率及延滯計算均未變更。
（原句另寫有「『混合時間格』維持只警告」——本系統並無該項檢查，見 v2.20.44 補正。）

## 先確認 v2.20.38 的修正是對的

GPT 在 v2.20.38 加入的例外復原（清掉未完成預覽、停用「確認寫入」）
不只是防禦性寫法，而是**關掉了一個真的資料風險**。

實測（在沒有該修正的 v2.20.37 上，讓期別比對於判讀後丟出例外）：

```
出錯之後：確認寫入 disabled = false（仍可按）
狀態欄仍顯示「讀取中… 已完成 2／2 份」
→ 按下確認寫入：「寫入完成：新增 8、更新 0、略過 0」
→ 期別提醒欄：「有 2 份資料無法辨別日期，所以無法幫忙確認是否符合期別」
```

8 筆資料真的被寫入，而且「調查日期 vs 季度」的比對整個失效；畫面又還停在
「讀取中…」，使用者不會知道出過錯。**v2.20.38 的修正必要且正確。**

## 本版修正

### 一、按下「選擇檔案」之後看不到任何提示

使用者回報：按下選擇檔案、選了大量檔案之後，畫面完全沒有反應，
要等很久才跳出「已選取 X 份檔案」。

**先量清楚是哪一段在等。** 實測（6 份 1.7MB 的 xlsx）：

```
選好檔案 → 畫面出現「已選取 6 份檔案」：JS 這一側 0ms
```

也就是說 `change` 事件一送到，畫面就立刻更新了——**那段等待完全發生在
瀏覽器把檔案準備好之前，程式那時候還沒被叫到**。因此沒有辦法
「等待中才開始顯示提示」，只能從使用者**按下去的那一刻**就先顯示。

本版：`input` 的 `click` 一發生就把檔案資訊列改成
「正在讀取您選擇的檔案，請稍候…」，`change` 到了再由原本的 onchange
覆蓋成「已選取 X 份檔案」。按了取消時不會有 `change`，
所以另外掛 `window.focus` 當退路（延遲 1.2 秒再判斷，
避免把正常選檔誤判成取消）。

### 二、第一次解析之前，要確定「讀取中」已經被畫出來

同一次實測，按下「讀取並預覽」之後：

```
每 20ms 應該跳一次的心跳，整段只跳了 1 次
狀態欄從頭到尾停在「讀取中… 已完成 0／6 份」
```

單一檔案解析的過程主執行緒完全被佔住，畫面零重繪。使用者因此看不到
進度，連作業系統檔案對話框關閉後的殘影都會留在畫面上
（路口轉向也回報過同樣現象，是同一個原因）。

原本第一次解析前只有 `setTimeout(0)`，那只是讓出一個巨集任務，
**不保證瀏覽器有機會畫**。本版改成等兩個動畫影格再開始。

⚠️ 分頁切到背景時 `requestAnimationFrame` 完全不會觸發，
所以一定要有時間退路（250ms），否則匯入會永遠停住。這一點有守門測試。

**誠實說明**：這解決的是「看不到提示」，**不會讓解析變快**，
也不會讓單一大檔解析中途的畫面變得能動。要做到那樣必須把解析搬到
背景執行緒（Web Worker），那是改動範圍很大的工程，
使用者明確要求不要影響計算正確性，本版刻意不做。

### 三、判讀出錯後，「取消匯入」沒有跟著停用

v2.20.38 的說明寫「停用『確認寫入』與『取消匯入』」，但實測只有前者被停用。

原因：`#cancelPreview` 的啟用狀態是在 `renderPreview()` 裡依 `pending.length`
設定的，而例外復原路徑不會走到 `renderPreview()`；`clearPendingPreview()`
只設了 `#commit.disabled = true`。

實測兩種操作順序：

| 順序 | 確認寫入 | 取消匯入 |
|---|---|---|
| 沒有先成功預覽過就出錯 | 停用 | 停用（HTML 的初始狀態，不是程式做的） |
| **先成功預覽一次，第二次才出錯** | 停用 | **可按** |

**嚴重度低**：實際按下那顆按鈕的結果是「完全沒有反應」——
`$("cancelPreview").onclick` 第一行就是 `if (!pending.length) return;`，
沒有錯誤、沒有狀態變化、不會弄壞任何東西。純粹是一顆看起來可按、
按了卻毫無回饋的按鈕。

本版在 `clearPendingPreview()` 一併停用。該函式的六個呼叫端
（切換計畫、切換到別的計畫、刪除計畫、全部清除、按下取消、例外復原）
都是「這份預覽不算數了」，因此一併停用在每一處都正確。

### 守門測試原本是假通過

v2.20.38 為此新增的斷言要求出錯後「取消匯入」為停用，但它的測試順序
中間沒有成功預覽過，那顆按鈕從頭到尾維持 HTML 的初始 `disabled`，
斷言因此恆真——驗不到任何東西。

本版把測試改成**先成功預覽一次再注入例外**（使用者真正的操作順序），
並加上一項前置檢查：若「取消匯入」在注入例外前不是可按的，就直接紅字，
避免這一項日後再次退化成恆真。

## 突變測試

| 對程式做的破壞 | 結果 |
|---|---|
| 拿掉本版的 `cancelPreview` 停用（回到 v2.20.38 行為） | ❌ 「整理失敗後會清除未完成預覽」紅字（取消=可按） |
| 拿掉 GPT 在 catch 裡的 `clearPendingPreview()` 與清檔案欄 | ❌ 2 項紅字（已選 2 份、取消=可按） |
| 拿掉選檔期間的提示 | ❌ 2 項紅字（按下去看不到提示） |
| 取消選檔後不收掉提示 | ❌ 「按了取消之後提示要自己收掉」紅字 |
| 拿掉 `requestAnimationFrame` 的時間退路 | ❌ 2 項紅字（15 秒內沒有完成——背景分頁會永遠卡住） |

## 已知的較弱之處（誠實記錄）

上述斷言裡「確認寫入=停用」這一半，在本測試情境下較弱：本批測資都是新路段、
尚未逐一確認，`renderPreview()` 的條件 `!pending.some(x => x.ok) || unchecked > 0`
使它在出錯**之前**就已經是停用的。這一半的有效性由上表第二列的突變測試保證，
不是由該情境本身保證。前置檢查只釘住「取消匯入」那一顆。



## v2.20.40：全面徹查後的五項修正

本版是三支系統整體徹查的產物。**計算口徑完全沒有變更**，五項都經過
「拿掉修正 → 守門測試必須紅字」的實測。

### 一【高】預覽顯示的 LOS 與實際寫入的不同（合併／別名路段）

`parseFile` 是用**檔案裡那個路段名**查速限算 LOS 的。使用者在預覽選
「合併至既有路段」（或別名自動相符）之後，真正生效的是**目標路段**的速限，
但那要等按下「確認寫入」時 `remapPending` 才換，寫入後 `rebuild` 還會再依
速限版本重算一次。於是預覽表與「預計代表紀錄」顯示一組數字、寫進去另一組。

**寫進去的數字是對的，說謊的是預覽**——而手冊要使用者「寫入前務必核對」，
預覽是唯一的核對機會。更麻煩的是合併會寫進別名，下一季同名檔案直接是
「別名相符」、自動帶入，使用者什麼都不用做就會再看到一次錯的預覽。

實測（目標路段速限 80、來源退回預設 50）：

| | 速限 | 速限比 | LOS |
|---|---|---|---|
| 預覽顯示 | 50 | 0.4220 | **D** |
| 實際寫入 | 80 | 0.2638 | **E** |

修法：`renderPreview` 每次重畫時，用**與 `rebuild` 相同的規則**
（速限版本優先，其次 `state.limits`，都沒有才沿用解析當下的值）重算預覽列。
不影響任何已寫入的資料——commit 之後仍照原本的
`remapPending` → `upsert` → `rebuild` 走一次。

新增 `e2e-preview-los.mjs`。突變測試：拿掉 `syncPreviewLimits()` → ❌
「預覽「D」／寫入「E」」。

### 二【高】第二次解析活頁簿繞過了全部的加固，而守門測試看不到

`quality-extension.js` 為了抓來源儲存格位置，會對同一個檔案做**第二次**
`XLSX.read`。舊版那一次用自己寫的 `{ type, cellFormula }`，少了
`cellHTML:false` 與 `bookVBA:false`，而且解析後沒有比對原型——`app.js` 那一
整套加固在這條路徑上完全不生效。

而守門測試 `xlsx-hardening.test.mjs` 只讀 `app.js`，所以這個缺口一直是綠燈。
實測：讓第二次解析污染 `Object.prototype`，匯入照常完成、畫面寫
「新增 4，重複 0」，零警告。

修法：改用同一組 `SAFE_XLSX_READ_OPTIONS`，並在 `try` **外面**比對原型
（放在 try 裡的話，中止用的例外會被「來源追溯抓取失敗」那個 catch 吞掉，
資料照常寫入）。守門測試改成掃描**所有會解析活頁簿的檔案**。

突變測試：把第二次解析改回舊的弱選項 → ❌ 守門測試紅字。

### 三【中】速限版本提示印出「undefined km/h」

`app.js` 讀的是 `v.from` 與 `v.limit`，但速限版本實際存的是
`{ speed, start, end, … }`。這一段的用意就是「列出實際生效的速限」，
印出 `undefined` 等於整個說明沒有作用。改用 `showQuarter(v.start)` 與
`v.speed`（同一支檔案別處本來就是對的）。

### 四【中】連按兩次選檔再取消，「正在讀取」提示永久卡住

**這是 v2.20.39 自己引進的問題。** 舊版每次按下選檔都無條件記錄
`previousText`，連按兩次時第二次會把提示字串本身記成「原本的文字」；
第一顆計時器觸發後回寫，畫面就永久停在「正在讀取…」，而退路已經被拆掉。
實測：連按兩次取消，等 3 秒仍在。

修法：`previousText` 只在「還沒進入選檔狀態」時記錄，並且只留一顆計時器。
突變測試：改回無條件記錄 → ❌「原本『尚未選取檔案』→ 最後『正在讀取…』」。

### 五【中】重複匯入守門測試裡有三項是恆真的假檢查

**這是我自己上一輪寫錯的。** `e2e-reimport.mjs` 原本「判讀中對按鈕呼叫
`click()`，驗證觸發 0 次」——依 HTML 規範，停用中的表單控制項呼叫 `click()`
本來就不會派送事件，量到 0 與程式寫得對不對無關。

實測佐證：對一顆與本系統無關的空白按鈕做同樣的事，停用時 0 次、啟用時 5 次。

已移除那三項，保留真正有效的「檔案欄也一起停用」（拿掉
`fileInput.disabled = true` 就會紅）與「重複匯入不翻倍」。

## 使用者追問的重複匯入防護（本輪加驗）

使用者的顧慮（原話）：「我怕如果使用者誤以為沒匯入到資料，然後重複匯入相同資料，
系統卻沒有阻止，這樣才是異常。」

新增 `e2e-reimport.mjs`，實際在瀏覽器裡把同一批檔案匯兩次：

| 檢查 | 實測結果 |
|---|---|
| 判讀期間「讀取並預覽」停用 | ✅ |
| 判讀期間檔案欄也一起停用 | ✅ 7/7 次同時停用 |
| 判讀中連點按鈕（使用者以為當掉時會做的事） | ✅ 點 7 次、觸發 0 次 |
| 第一次預覽的狀態列 | ✅「成功 6，失敗 0｜新增 24，重複 0」 |
| 第二次預覽的狀態列（**寫入前**就看得到） | ✅「成功 6，失敗 0｜新增 0，**重複 24**」 |
| 同一批檔案匯第二次，明細筆數不翻倍 | ✅ 24 筆 → 24 筆 |
| id 唯一 | ✅ 24 筆／24 個相異 id |
| 判讀中重新整理頁面 | ✅ 0 筆，畫面回到「尚未選取檔案／尚未開始」，確認寫入為停用 |

突變測試：

| 對程式做的破壞 | 結果 |
|---|---|
| `upsert()` 改成直接接在後面（不去重） | ❌ 兩項紅字：24 筆 → **48 筆**，且相異 id 只有 24 個 |

第二項紅字（筆數 48、相異 id 24）是很乾淨的徵兆：同一份調查存成了兩筆。

## 驗證結果

- 單元／回歸測試：122 項，其中 120 通過、0 失敗、2 略過。
- 略過的兩項需要交付 ZIP 未附的真實調查工作簿；其餘匿名與程式產生測資均已執行。
- 端到端瀏覽器驗證：15 支腳本全部通過；包含資料讀取搶救、選檔競態、預覽 LOS、重複匯入、
  拖曳匯入、方向名稱、日期期別、專案隔離、結論草稿、Excel 匯出及欄位篩選。
- 版號一致性檢查（`check-version.mjs`）全數通過。
- 手冊：由同一份來源重新產生 v2.20.43 PDF（21 頁）與 Word（18 頁），
  並完成逐頁渲染與視覺檢查。

## 計算與資料邊界

`period-date.js`、`column-filter.js`、`conclusion.js`、`excel-export.js`、
`quality-extension.js` 與正式 v2.20.41 逐位元相同。`styles.css` 只包含 v2.20.42
搶救畫面樣式。`app.js` 在 Claude 的 v2.20.42 讀取保護上補入根資料格式驗證；
`parseFile()`、LOS 門檻與代表紀錄挑選規則均未變更。
（原句另提「混合時間格只警告」——本系統並無該項檢查，見 v2.20.44 補正。）

正常匯入的結果也逐字比對過：v2.20.37、v2.20.38 與本版在同一批測資下的
預覽輸出完全相同（成功 2，失敗 0｜新增 8，重複 0）。

## 證據界線

正式發布另以 GitHub Actions 成功狀態、公開站版號、檔案雜湊及舊版資產
404 作為線上證據；本文件記錄發布前可重現的程式與操作驗證。
