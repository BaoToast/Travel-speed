# 交通服務水準分析系統 v2.20.13 驗證紀錄

日期：2026-08-28

## 本版範圍

- **沒有變更任何計算規則、判定門檻、匯入方式或備份格式。**
- v2.20.13 只修一件事：**v2.20.12 把搜尋範圍縮掉了一塊。**

### 症狀與成因（v2.20.13 修的）

在「尖峰明細」「尖峰彙總」或 Manager 的搜尋框輸入 `A16` 或 `平均總旅行速率`，
v2.20.12 回 **0 筆**，畫面上沒有任何跡象說明原因——看起來像是這筆資料不存在。

v2.20.12 的 `rowSearchText()` 為了排除內部物件（會被串成 `[object Object]`，
讓搜「object」列出全部資料），改成**只收錄字串、數字與布林值**。
但資料列裡的 `sourceRefs` 是**陣列**，不是物件——它是每筆數值取自原始 Excel
哪幾格的追溯清單，字串化的結果（`平均總旅行速率:A7,路段延滯:A3…`）是有意義、
原本就搜得到的文字。「排除非純量」比「排除物件」範圍大，把它一起掃掉了。

不影響任何交通數值、服務水準判定或匯出內容；影響的是**稽核與追溯**：
要用儲存格位置或原始欄位標題回頭找是哪幾列時，查不到任何東西。

### 修正（v2.20.13）

`rowSearchText()` 改為「**排除物件，但保留純量陣列**」：

```js
const scalar = (v) => ["string", "number", "boolean"].includes(typeof v);
Object.values(row).forEach((value) => {
  if (scalar(value)) values.push(value);
  else if (Array.isArray(value) && value.every(scalar)) values.push(value.join(" "));
});
```

v2.20.11 的 `[object Object]` 修正與 v2.20.12 的方向顯示名稱搜尋皆完整保留。

## 守門檢查對未修正版（v2.20.12）的實測

| 檢查 | v2.20.12（未修正） | v2.20.13 |
| --- | --- | --- |
| 尖峰明細可用來源原始欄位標題搜尋（`平均總旅行速率`） | ❌ 0 筆 | ✅ 8 筆 |
| 尖峰明細可用來源儲存格位置搜尋（`A16`） | ❌ 0 筆 | ✅ 8 筆 |
| 尖峰彙總可用來源原始欄位標題搜尋 | ❌ 0 筆 | ✅ 2 筆 |
| 尖峰彙總可用來源儲存格位置搜尋 | ❌ 0 筆 | ✅ 2 筆 |
| 搜「object」不會列出全部資料（v2.20.11 的修正沒被弄壞） | ✅ | ✅ |
| 三個搜尋入口都吃得到方向顯示名稱（v2.20.12 的修正沒被弄壞） | ✅ | ✅ |
| 原本用路段名稱搜尋仍有效 | ✅ | ✅ |

---

## 上一版（v2.20.12）範圍

- **沒有變更任何計算規則、判定門檻、匯入方式或備份格式。**
- v2.20.11 修正 Manager 比較搜尋方向顯示名稱；v2.20.12 再補齊 Project 的「尖峰明細」與「尖峰彙總」搜尋。

### 症狀與成因

Manager、尖峰明細或尖峰彙總的表格裡印著「南-北(北上)」，把它打進搜尋框卻可能得到沒有資料。

Manager 顯示名稱由 `managerDirectionName()` 從專案包的 `roadMeta` 即時解出；
Project 顯示名稱則由 `rowDirectionName()` 從路段管理設定解出。舊搜尋都只把
原始資料列的欄位串起來，畫面上即時解析出的方向名稱不在可搜尋文字中。
Manager 的內部物件還會被串成 `[object Object]`，造成搜「object」列出全部資料。

### 修正

新增共用 `rowSearchText()`：只納入字串、數字與布林欄位，再加入畫面實際顯示的
方向名稱。尖峰明細、尖峰彙總與 Manager 都走同一套純量搜尋文字；Manager 另由
`managerSearchText()` 排除 `packageProject`、`packageRoadMeta` 兩個內部物件。

**這是「多加一項」，不是「改成只搜畫面上的欄位」**——站號、資料別這些
沒有印在表格上但一直搜得到的欄位，仍然搜得到（有回歸檢查釘住）。

這與前幾版是同一族缺陷（畫面顯示 X、功能操作 Y）：顯示、匯出 CSV 與三個
搜尋入口現已全部使用一致的方向顯示名稱。

## 上一版（v2.20.12）守門檢查對 v2.20.10 的實測

| 檢查 | v2.20.10 | v2.20.12 |
| --- | --- | --- |
| Manager 搜尋框搜得到畫面上顯示的方向名稱 | ❌ 目前篩選條件沒有資料 | ✅ |
| 搜「object」不會列出全部資料 | ❌ 列出全部 | ✅ |
| 原本用路段名稱搜得到的仍然搜得到 | ✅（回歸檢查，兩版都該過） | ✅ |
| 清空搜尋後資料回來 | ✅（回歸檢查，兩版都該過） | ✅ |
| 尖峰明細搜尋框搜得到方向顯示名稱 | ❌ | ✅ |
| 尖峰彙總搜尋框搜得到方向顯示名稱 | ❌ | ✅ |
| 尖峰明細原本的路段名稱搜尋仍有效 | ✅ | ✅ |
| 尖峰彙總原本的路段名稱搜尋仍有效 | ✅ | ✅ |

## 完整驗證結果

- 單元測試：55/55 通過。
- 版本一致性檢查：18/18 通過。
- 瀏覽器操作測試：4 支腳共 **169 項**全部通過（v2.20.11 為 165 項，本版新增 4 項），沒有未處理的 JavaScript 例外。
- 舊版 Excel：`.xls` 尖峰資料辨識成功；`.xlsx` 辨識成功。
- 正式調查檔抽樣：5/5 與原檔彙總表一致，0 筆差異。
- 代表紀錄延滯固定案例：旅行速率 20.528、行駛速率 34.732、同筆總延滯 132 秒，測試通過。
- Excel 讀取元件：瀏覽器端與測試端均固定使用官方 SheetJS 0.20.3。
- 套件安全檢查：0 個已知弱點。
- 新手手冊：PDF 30 頁逐頁檢視，無空白頁、重疊或截斷；DOCX 結構檢查通過。

## 發布限制與資料保存

- 本系統是純瀏覽器網站；使用者匯入的正式資料保存在該瀏覽器本機，不會隨網站程式發布。
- 正式發布管道只有 GitHub Pages；不同電腦或不同瀏覽器不會自動共享資料。
- 完整專案備存包不含正式調查檔、Project 備份或個人資料。
- 本專案不使用 GPT Site，不需要且不包含 `.openai/hosting.json`。

## v2.20.2 追加驗證（2026-08-24）

本版沒有變更任何計算規則、判定門檻或操作流程，v2.20.1 的驗證結果全部沿用；
以下是本版新增項目的驗證：

- 單元測試：29/29 通過（v2.20.1 的 25 項加上 `xlsx-hardening.test.mjs` 4 項）。
- 版本一致性檢查：14 項全部通過。
- 瀏覽器操作測試：120 項通過、0 項失敗，涵蓋 640～1920 像素共 9 種寬度。
- 原型污染防護測試確認：污染會被偵測、刪除，並中止該檔案的匯入。

## v2.20.3 追加驗證（2026-08-25）

本版沒有變更任何計算規則、判定門檻或操作流程。修正的是備份範圍：
結論草稿的條件範本納入專案包與個人全部計畫包。

- 新增 `backup-completeness.test.mjs` 4 項。
- 既有測試全部重跑，未出現迴歸。

## v2.20.4 追加驗證（2026-08-26）

本版沒有變更任何計算規則、判定門檻或操作流程。修正的是手冊版號與日期的來源。

- 新增 `manual-src/release.mjs`，頁尾字樣與手冊檔名一律由封面戳記推導。
- `check-version.mjs` 由 14 項增為 18 項，並實測新增檢查能攔截寫死日期與版號不一致。
- 既有測試全部重跑，未出現迴歸。

## v2.20.5 追加驗證（2026-08-26）

本版只清理維護文件，沒有變更程式的計算、匯入、備份或操作邏輯。

- 刪除未被程式引用的 `給Claude的交接說明_v2.20.1.md`。
- README、驗證紀錄、更新說明與手冊統一為 GitHub Pages 單一發布管道。
- 完整重跑單元測試、版本一致性檢查與瀏覽器操作測試，結果分別為 41/41、18/18、120/120。

## v2.20.6 追加驗證（2026-08-27）

本版只改顯示名稱的解析，沒有動任何計算。

- 新增 `e2e-direction-name.mjs`：實際在瀏覽器裡替路段的方向命名，再逐一檢查
  尖峰明細、尖峰彙總、路段速限、結論草稿產生器的勾選標籤、結論草稿內文、
  報告文字草稿、資料維護、匯出 CSV 與 Manager 比較有沒有跟著換，
  並確認命名後該路段任何地方都不再出現裸的「方向1／方向2」。
- **守門檢查已實測會在未修正的舊寫法下失敗**：把 v2.20.5 的原始碼配上本版測試，
  端對端 8 項紅字（路段管理用詞、尖峰明細、勾選標籤、結論草稿兩項、CSV、Manager 兩項）、
  單元測試 5 項紅字、原始碼守門 4 項紅字，逐一對應到實際缺陷的位置。
  修正後全部通過。（只加會通過的檢查等於沒加檢查。）
- 另外兩項是**回歸守門**，在新舊兩版都通過：沒有 directionLabel 時維持鍵值（舊資料行為不變）、
  方向篩選用的仍然是鍵值（存好的條件範本不會失效）。
- 手冊 PDF 27 頁、無空白頁；封面戳記與每頁頁尾皆為 `v2.20.6 ｜ 2026-08-27`。

## v2.20.7 追加驗證（2026-08-27）

本版只修一個顯示判斷，沒有動任何計算。

- **使用者回報**：Manager 比較裡，同一個計畫有些路段顯示改好的方向名稱、有些還是「方向1／方向2」。
- **成因**：判斷「有沒有取過名字」時只看「roadMeta 有沒有這筆項目」，沒看「名字是不是還是預設值」。
  而設定路段有效期間時會寫進 `{directionA:"方向1", directionB:"方向2", startPeriod, endPeriod}`
  ——那筆項目「有值」，於是擋掉了本機同一個計畫真正取好的名稱。所以是**逐路段**的：
  哪些路段設過有效期間，哪些就顯示不出來。
- **改法**：`hasRealDirectionName()` 判斷「真的取過名字」；`managerDirectionName()` 改為
  專案包 → 本機 → 鍵值 的順序，**挑真的有名字的那一份**。
- **已實測會在 v2.20.6 下紅字**：把本版的守門測試放到 v2.20.6 的原始碼上，
  「Manager 比較不會被『有項目但沒名字』的佔位值擋住本機取好的名稱」該項失敗，
  實際輸出 `示範路B(起~迄)|方向1|方向1`，與使用者截圖的症狀一致。
- 此項修正只適用於專案包內仍是佔位值、本機已有正式名稱的情境。
  若 Manager 裡匯入的專案包本身保存的就是舊正式名稱，仍必須從 Project 重新匯出並匯入 Manager。
- 單元測試 54/54、版本一致性檢查 18/18、瀏覽器端對端 147 項全過、手冊 27 頁無空白頁。

## v2.20.9 追加驗證（2026-08-28）

- **發現的交付問題**：v2.20.7 壓縮包又包含 `給Claude的交接說明_v2.20.1.md`，但 README、驗證紀錄與更新說明都記載該過時檔案已移除；檔案內容仍描述已停用的 GPT Site 維護方式。
- **修正**：移除過時檔案，修正更新說明末段的舊版號與刪除清單，新增「交付包不可再帶入已過時的 GPT Site 交接說明」單元測試。
- **負向實測**：把過時檔案暫時放回根目錄時，新測試會失敗；移除後通過。
- **計算邏輯差異檢查**：v2.20.7 與 v2.20.9 的服務水準門檻、代表紀錄選取、旅行速率、行駛速率、總延滯、匯入、備份與還原程式無差異。
- **完整回歸結果**：全新安裝後，單元測試 55/55、版本一致性檢查 18/18、瀏覽器端對端 147/147 全部通過；手冊 PDF 28 頁逐頁檢視，無空白頁、重疊或截斷。

## v2.20.11 追加驗證（2026-08-28）

- Manager 畫面、尖峰明細、尖峰彙總、路段速限、結論草稿、健康檢查與 CSV 匯出的方向名稱一致。
- Manager CSV 同時保留原始 `direction` 鍵值與人員可讀的 `directionLabel`，不含內部 `packageRoadMeta` 物件。
- 本機與 Manager 專案包只有 `directionText` 不同時，過期提醒仍會正確出現；內容一致或本機沒有該計畫時不誤報。
- 程式註解、新手手冊與驗證紀錄均已改為經使用者實測證實的正確因果。

## v2.20.12 追加驗證（2026-08-28）

- 尖峰明細、尖峰彙總與 Manager 三個搜尋入口都能依畫面上的方向顯示名稱查詢。
- 尖峰明細與尖峰彙總原本依路段名稱搜尋的行為維持不變。
- 搜尋文字只納入純量欄位，避免任何內部物件被轉成 `[object Object]`。
  （⚠️ 此作法一併排除了純量陣列 `sourceRefs`，已於 v2.20.13 修正。）
- 本次只修正搜尋一致性，計算、門檻、代表紀錄、匯入與備份程式未變更。

## v2.20.13 追加驗證（2026-08-28）

- 尖峰明細、尖峰彙總與 Manager 都恢復可用 `sourceRefs`（資料來源）搜尋。
- 新增 4 項守門檢查，**已對 v2.20.12 實測 4 項全紅**。
- 單元測試 55/55；版本一致性 18/18。
- `e2e-speed`／`e2e-conclusion`／`e2e-direction-name`／`e2e-layout` 四支全過，0 個 JS 例外。
- 版面 640～1920 px 九個寬度全分頁無橫向溢出。
- 固定代表紀錄案例仍為旅行速率 20.528、行駛速率 34.732、同筆總延滯 132 秒。
- 手冊 PDF／DOCX 依 v2.20.13 封面戳記重新產生，`manuals` 無殘留舊版。
- 本次只修正搜尋範圍，計算、門檻、代表紀錄、匯入與備份程式未變更。
