# 交通服務水準分析系統工程交接基準

> 本文件是本 Repository 唯一的長期工程交接文件。未來換 GPT 開發對話時更新本檔，不另建 `PROJECT_HANDOFF_2.md` 等編號副本；歷史由 Git 保存。

## 0. 文件狀態與事實來源

- 本文件建立日期：2026-09-13（Asia/Taipei）。
- 本次只建立工程交接文件，沒有修改正式程式功能、交通工程計算或部署設定。
- 功能正式版基準 commit（建立本文件前的 HEAD）：`54a4624ae08c570007cf0f888dd7435e6ffc22c1`，訊息為 `Release Travel-speed v2.20.54`。
- 本交接文件本身的 commit 無法在 commit 建立前自我引用；新接手者應以 `git log -1 --oneline` 取得包含本文件的最新 HEAD，並以本節的 `54a4624...` 作為程式功能基準。
- 事實優先順序：目前可重現的 Repository／程式碼／Git／測試結果，高於舊聊天、舊交接文字、README 或其他說明。若彼此衝突，必須指出差異；無法驗證者標示「待確認」，不得猜測。

## 1. 正確程式與 Repository 身分

### 1.1 已實際核對

| 項目 | 核對結果 |
|---|---|
| 程式正式名稱 | 交通服務水準分析系統 |
| 英文／識別名稱 | Travel-speed；Traffic LOS Manager |
| 用途 | 匯入路段平均旅行速率調查 Excel，保存尖峰四筆方向資料，計算服務水準，提供單一計畫分析與 Manager 跨計畫比較 |
| 本機資料夾名稱 | `repo` |
| 本機完整路徑 | `D:\Users\95108\Documents\Codex\2026-08-27\1\review_v253_0909\repo` |
| GitHub Repository | `BaoToast/Travel-speed` |
| Repository URL | `https://github.com/BaoToast/Travel-speed` |
| Git origin（fetch／push） | `https://github.com/BaoToast/Travel-speed.git` |
| 正式 GitHub Pages | `https://baotoast.github.io/Travel-speed/` |
| branch | `main` |
| 功能正式版基準 commit | `54a4624ae08c570007cf0f888dd7435e6ffc22c1` |
| 目前程式／正式發布版本 | `v2.20.54` |
| 版本唯一來源 | `app.js` 內的 `APP_VERSION`；`check-version.mjs` 會檢查 HTML、手冊、驗證檔及資產 query string 一致性 |

本機資料夾名 `repo` 很通用，不能只靠名稱判斷；每次接手都必須同時核對完整路徑、`.git`、origin、branch、`app.js` 版本與 GitHub Repository。三個交通系統不得混用資料夾或規則。

### 1.2 部署邊界

- 正式發布只使用 GitHub Pages。
- Pages 由 GitHub `Settings → Pages → Deploy from a branch` 直接發布 `main` 根目錄。
- `.github/workflows/ci.yml` 只負責 Node 22、`npm ci`、`npm test`，**不負責部署**。不要再新增另一條 Pages deploy workflow，否則兩條部署流程可能競速並使線上內容倒退。
- 本專案沒有、也不得為了發布自行新增 `.openai/hosting.json`。
- 本系統的 GPT Site 已由使用者刪除；除非使用者日後明確要求重新建置，後續不用發布或回報 GPT Site。

## 2. 系統用途、使用情境與完成程度

本系統是純前端、可離線運作的單頁網站。每位同事可在自己的瀏覽器建立多個 Project，匯入各季度路段平均旅行速率調查檔、確認速限、檢查品質、製作圖表與成果包；管理者則將各 Project 匯出的 JSON 專案包匯入 Manager 做跨計畫比較。

目前主體、路段管理、速限版本、LOS、健康檢查、趨勢、結論草稿、成果匯出、Manager、備份／還原與新手手冊均已完成。v2.20.54 已完成一次高風險全面複查並正式發布。現階段工作原則是：修正錯誤、補缺漏功能、驗證結果；不要為未來平台整併而重構，也不要改變已確認計算規則。

資料不會上傳伺服器，存於目前網域、目前瀏覽器的 IndexedDB。同一網址在另一台電腦、另一個瀏覽器或另一個網域不會自動看到原資料；清除網站資料也會移除資料。換電腦或清除前必須下載 Project／全部計畫備份。

## 3. 技術架構與主要檔案

### 3.1 技術型態

- 純靜態 HTML／CSS／原生 JavaScript，無框架、無 bundler、無 production build。
- `index.html` 依序載入本機 vendor、匯出、期別、篩選、趨勢、主程式、結論及品質擴充模組。
- IndexedDB：資料庫 `TrafficLOSWebV2`、object store `app`、key `state`。
- state schema 目前為版本 10。
- 所有外部資料在寫入 HTML 前必須經 escaping；不要直接拼入未處理的檔名、路段名、計畫名或專案包文字。

### 3.2 主要檔案與責任

| 檔案 | 責任與重要依賴 |
|---|---|
| `index.html` | 頁面骨架、主要功能區、腳本載入順序、靜態版本 query string |
| `styles.css` | 全站版面、響應式、表格、圖表及錯誤／安全提示樣式 |
| `app.js` | state、IndexedDB、Project、Excel parser、LOS、代表紀錄、匯入預覽／寫入、路段／方向、速限、圖表、Manager、備份還原、健康檢查的主協調器 |
| `period-date.js` | 民國／西元季度正規化、調查日期解析、季度相符檢查、顯示格式；此模組與另外兩套交通系統有共用契約，修改時需評估三套一致性 |
| `column-filter.js` | 表頭漏斗、多選條件、篩選狀態與清除行為 |
| `trend.js` | 趨勢唯一計算來源；圖、說明與 Excel 必須共用 `buildTrendSeries()`／`buildBandSeries()` 的結果 |
| `trend-excel.js` | 可編輯趨勢 Excel 組裝；依季度聯集對位，缺季保留空白 |
| `excel-export.js` | OOXML 工作簿／圖表匯出共用工具；需維持舊版 Excel 可開啟的元素順序 |
| `conclusion.js` | 結論草稿資料挑選與文字產生；只讀已計算資料，產出必須人工核對 |
| `quality-extension.js` | 來源追溯、匯入品質、速限版本、異常規則、成果交付、操作復原等擴充；其二次 Excel 解析必須使用與主 parser 相同的安全選項 |
| `vendor/xlsx.full.min.js` | SheetJS 0.20.3 瀏覽器版，Excel 讀取 |
| `vendor/jszip.min.js` | 成果 ZIP 匯出 |
| `manual-src/*` | 手冊單一來源與 PDF／DOCX 產生器 |
| `manuals/*v2.20.54*` | 當版可下載新手手冊；正式包只能保留當版一組 |
| `*.test.mjs` | Node 單元、契約、回歸與安全測試 |
| `e2e-*.mjs` | Playwright 瀏覽器端流程測試 |
| `generate-test-fixtures.mjs` | 產生 6 份匿名 Excel 測資供 E2E 使用；不等同實際業務檔案驗證 |
| `check-version.mjs` | 版本、資產引用、手冊及交付文件一致性檢查 |
| `ooxml-check.mjs` | 檢查匯出 Excel 的 OOXML 結構與元素順序 |
| `VALIDATION_v2.20.54.md` | v2.20.54 的實際複查與測試證據摘要 |
| `.github/workflows/ci.yml` | GitHub Actions 測試門；不部署 |

### 3.3 state 主要資料結構

`emptyState()` 目前包含：

- `projects`、`activeCode`：多計畫與目前計畫。
- `details`：每個路段／季度／日別／尖峰／方向的完整明細。
- `summaries`：每路段／季度／日別的一筆最差代表紀錄。
- `limits`、`limitConfirmed`、`speedVersions`：方向速限、確認狀態與有效期間版本。
- `aliases`、`roadMeta`：檔名別名、正式路段資訊、方向顯示名、有效期間。
- `losRules`：各 Project 的 A～E 最低速限比門檻。
- `bandRules`：各 Project 的順暢／尚可／壅塞三段分界。
- `managerBandRule`：Manager 自己的三段分界，與各 Project 完全隔離。
- `anomalyRules`、`reportDrafts`、`conclusionTemplates`：品質門檻、報告／結論設定。
- `imports`、`operations`：匯入批次與可復原操作。
- `periodDisplay`、`yearStyle`：只影響顯示／匯出文字，不改內部鍵值或計算。
- `manager`：匯入 Manager 的 Project 專案包快照。

## 4. 完整資料流程

1. **建立／選擇 Project**：計畫編號與名稱建立作用範圍；所有設定與資料必須依 `projectCode` 隔離。
2. **輸入季度**：接受民國 90～200 或相應西元 2001～2111，內部一律正規化為民國 `115Q2`。季度是資料鍵與分組依據。
3. **選取或拖曳 Excel**：`.xls`、`.xlsx`、`.xlsm`，可一次多檔；拖曳會把檔案交給原本 file input 的同一條 change 流程，不另寫第二套 parser。
4. **安全讀取**：SheetJS 選項關閉公式、HTML、VBA；讀取前後比較 `Object.prototype`，偵測到污染即清理並中止該檔。
5. **識別路段／日別／日期**：工作表表頭優先，檔名備援；來源衝突要顯示警告，不能靜默猜測。
6. **識別尖峰工作表**：上午接受「上午尖峰／上午／AM尖峰／AM」，下午同理。兩張都必須存在。
7. **解析記錄區塊**：每張尖峰表應恰有兩個方向，抽取旅行速率、行駛速率、路段延滯、交叉口延滯與報告方向文字。
8. **驗證**：每檔應有 4 筆（上午／下午 × 方向1／方向2）；必要數值不可缺；旅行速率與行駛速率須為正，旅行速率不得大於行駛速率；有延滯時兩速率不應相等。歧義、多標籤、同方向重複、來源衝突或日期不符均要明示。
9. **預覽而不寫入**：新路段／既有路段／別名／疑似相符狀態、來源、錯誤、速限及預估 LOS 先顯示；寫入按鈕必須由有效 pending context 控制。
10. **確認寫入**：同一資料鍵採 upsert；保存匯入批次、來源檔、工作表／儲存格追溯及雜湊。重新匯入既有季度依使用者選擇更新，不能無聲累加。
11. **重建彙總**：先按當期有效速限及該 Project LOS 門檻重算明細，再從四筆中選一筆代表紀錄。
12. **呈現／分析**：明細、彙總、路段速限、LOS 圖表、趨勢、三段組成、品質檢查、結論草稿及 Manager 均從 state 的同一批資料衍生。
13. **匯出**：CSV、Project JSON、全部計畫 JSON、Manager 篩選結果、成果 ZIP、可編輯 Excel 圖表與手冊。

## 5. 不可任意改變的交通工程規則

### 5.1 單位與公式

- `travel` 平均總旅行速率：km/h。
- `running` 平均總行駛速率：km/h。
- `roadDelay` 路段延滯：秒。
- `junctionDelay`（部分歷史欄位可能名為 `intersectionDelay`）交叉口延滯：秒。
- `totalDelay = roadDelay + junctionDelay`，缺任一項即為 `null` 並使該檔解析失敗；**不得把缺值當 0**。
- 公告速限 `limit`：km/h。
- `ratio = travel / limit`。LOS 只用旅行速率，不用行駛速率。
- 預設 LOS：A ≥ 0.80；B ≥ 0.60 且 < 0.80；C ≥ 0.50 且 < 0.60；D ≥ 0.40 且 < 0.50；E ≥ 0.20 且 < 0.40；F < 0.20。
- LOS 門檻可由各 Project 調整並隨專案包移轉；A～E 必須由高到低，F 是低於 E，不另輸入。
- 無有效速限比時 LOS 為 `?`，不得判成 F。

### 5.2 代表紀錄

每個 `projectCode + 年 + 季 + 路段 + 日別` 應有上午／下午及方向1／方向2共 4 筆。代表紀錄排序固定為：

1. LOS 最差；
2. 同級時速限比最低；
3. 再同時旅行速率最低。

選中後，旅行速率、行駛速率、路段延滯、交叉口延滯、總延滯、代表尖峰、方向、速限、速限比與 LOS 必須整筆取自同一筆明細。不得分別從四筆挑各欄最差值，也不得平均或混搭。

### 5.3 趨勢與三段統計

- `state.summaries` 已是一季一路段一日別一筆代表紀錄；趨勢不可回頭重判 LOS。
- 平日與假日分開畫、分開計算，不相加成一個「日」。
- 缺季保留 `null`／空白／斷線，不補 0。
- 平均旅行速率、行駛速率、總延滯、速限比，對該期有有效數值的代表紀錄作算術平均。
- 最差 LOS 是該期所有有效代表紀錄最差等級。
- 壅塞佔比分母只含可判定 A～F 的紀錄；未知 `?` 另列，不能當作不壅塞稀釋分母。
- 預設三段為順暢 A–B、尚可 C–D、壅塞 E–F；單一 Project 與 Manager 有各自獨立分界。
- 圖、圖旁說明與 Excel 必須使用 `trend.js` 同一次計算結果，禁止各自重算。
- Manager 跨計畫比較以比例／平均等可比指標呈現，不把不同計畫路段總量直接相加當成同一母體。

### 5.4 精度與顯示

- 計算保留 JavaScript 原始數值；`fmt` 或趨勢設定只在畫面／文字輸出四捨五入。
- 不得把已四捨五入的顯示值回寫成計算基礎。
- CSV／Excel 的單位與欄名必須清楚；旅行／行駛速率為 km/h、延滯為秒、佔比為 `%`。
- 民國／西元與「季度／實際月份」切換只改標籤；內部 period 永遠是民國季度鍵。

### 5.5 本系統不適用的計算

本系統不計算車種組成、PCU／PCE／當量係數、全日交通量、路口轉向量或各車種分類。這些屬於另外兩套交通系統，**不得把其公式、欄位或 parser 規則帶入本 Repository**。若未來使用者要求新增，必須先確認是否其實選錯程式。

## 6. Excel／CSV 輸入輸出與 Parser 契約

### 6.1 Excel 輸入

- 可輸入 `.xls`、`.xlsx`、`.xlsm`；CSV 不是原始調查匯入格式。
- 路段名優先取表頭前 12 列的「站名：」，取不到才解析檔名。
- 日別優先取表頭日期欄括號的「平日／假日」，取不到才解析檔名結尾。
- 日期掃描不依固定欄位；只供顯示與季度檢查，不參與速率、延滯或 LOS。
- 表頭與檔名都有值但不同時，採表頭並顯示衝突，不能靜默吞掉。
- 檔名備援建議：`<案號>TS<站號>-<路段名稱>-平日.xlsx`；案號剝除只接受既定格式。下載副本 `(1)`、`複本 -`、日期尾碼、全半形與標點差異可能形成新路段，預覽／路段管理／健康檢查需介入。
- 名稱正規化可處理部分全半形、空白與符號差異；唯一相符才可自動映射，歧義時必須讓使用者選擇新建或併入。
- 路段別名是明確對應，正式名稱與方向顯示名存於 `roadMeta`；方向鍵值仍固定為 `方向1／方向2`。
- 每張尖峰表應剛好有兩個「平均總旅行速率」方向區塊。多或少都報錯；不得猜哪兩個是正確方向，也不得自行平均。
- 每個方向都需「平均總旅行速率」「平均總行駛速率」「路段延滯」「交叉口延滯」。數值搜索不得越過下一個非空文字欄位或記錄區塊。
- 當工作表只剩公式而沒有 cached value，瀏覽器端可能讀不到數值；使用者需先用 Excel 開啟、重新計算並儲存，再預覽。
- 同批同一路段／日別／季度碰撞要阻擋或明示；Project 切換、重讀失敗或預覽條件改變時要清除舊 pending，禁止把上一批資料寫入新計畫。

### 6.2 速限

- 未設定時可用匯入預設 50 km/h，但必須標為未確認；既有路段速限不可因匯入另一季被預設值覆蓋。
- 速限可以依起始季度建立版本；預覽、正式重建、圖表與匯出必須使用該期有效版本。
- 修改／刪除速限版本後只重算受影響季度，但所有衍生結果都需同步。
- 快慢車道速限不同時，依實際調查車流使用的道路／車道設定，不能由程式猜測。

### 6.3 輸出

- 尖峰明細、尖峰彙總及 Manager 篩選結果可匯出 UTF-8 BOM CSV。
- CSV 欄位需使用聯集，不能以第一列欄位截掉後續 provenance 欄位；方向輸出需使用正式顯示名並保留可追溯資訊。
- Project JSON：`kind: TLM_PROJECT_PACKAGE`，只含目前 Project；同代碼還原只覆蓋該計畫，其餘保留。
- 全部計畫 JSON：`kind: TLM_PORTFOLIO_PACKAGE`。
- Manager 接收 Project JSON 快照；同計畫代碼重新匯入為更新，不重複累加。
- 成果 ZIP 可含明細、彙總、圖表、說明與可編輯 Excel。ZIP 只留當次生成資產。
- Excel 圖表採 OOXML；元素順序不符時舊版 Excel 會提示修復甚至刪除圖表。`ooxml-check.mjs` 與實際 Microsoft Excel 開啟檢查都很重要，LibreOffice 能開不能取代 Excel 相容性證據。
- 可編輯趨勢 Excel 要取所有數列季度聯集並逐季精確對位，缺值留空；不能把不同數列的第 N 點硬貼到同一季度。

## 7. 已確認 UI／UX 與防呆決策

- 匯入一定先預覽再寫入；預覽前不改資料。
- 選檔與拖曳共用同一流程、同一副檔名過濾與錯誤提示。
- 大量檔案解析要顯示實際進度與目前檔名，並定期讓瀏覽器重繪；這改善回應感，不代表解析速度變快。
- 輸入檔處理中鎖住計畫、季度及相關操作，並以 `try/finally` 保證 UI 解鎖。
- Parse 失敗後清除 pending 並停用確認／取消，不能留下舊成功批次可誤按。
- 新路段、疑似名稱、別名相符、既有路段的辨識信心要可見；沒有新路段時不要顯示冗長既有路段警告。
- 路段管理可改正式名、方向 A/B 顯示名、別名、有效期間及合併重複路段；合併／改名先顯示影響季度、筆數與碰撞，再下載備份並確認。
- 方向 `方向1／方向2` 是永久鍵值；自訂方向只改顯示。全站表格、CSV、結論、健康檢查與 Manager 必須走同一個方向名稱解析入口。
- Manager 不會自動同步目前 Project；必須重新匯出 Project 專案包再匯入。若本機同代碼 Project 已變更，Manager 應顯示過期提醒，但不能自動覆蓋交付快照。
- Project 與 Manager 的三段分級設定隔離；不得互相套用。
- 自動結論／報告只是草稿，正式交付前必須人工核對。
- 危險操作（合併、刪季、刪計畫、淨空、復原）需清楚說明範圍；適用時先自動下載備份。復原不得覆蓋後來重新匯入的資料。
- IndexedDB 載入失敗時鎖住 save 並顯示搶救流程，絕不能換成空白 state 後讓使用者儲存；儲存空間被封鎖與資料結構損壞需顯示不同指引。
- 頁面所有外部字串必須 HTML escape。
- 新手手冊須支援完全不熟交通調查者，並與當版功能／版本一致。

## 8. 重要技術決策與原因

1. **純靜態、資料留在瀏覽器**：不需後端與帳號即可部署，符合個人分散管理；代價是不同裝置不自動同步，容量受瀏覽器／裝置限制，必須備份。
2. **Project／Manager 分離**：Project 是持續編修的工作資料，Manager 是有時間點與交付者的快照；自動同步會破壞資料來源責任。
3. **同一代表紀錄整筆帶入**：避免旅行速率、行駛速率與延滯分別取自不同尖峰／方向而形成不存在的組合。
4. **明確失敗優於猜測**：parser 遇到標籤或方向歧義直接報錯；錯讀一個合理數字比顯示失敗更危險。
5. **計算與顯示分離**：民國／西元、季／月份、方向名稱、格式化精度不改鍵值與原始計算。
6. **趨勢單一資料來源**：圖、文字、Excel 共用 `trend.js`，避免同一圖旁出現不同數字。
7. **依賴離線保存**：正式網站不仰賴外部 CDN；SheetJS／JSZip 在 `vendor/`。
8. **CI 與部署分離**：Actions 只測試，GitHub Pages 分支部署只一條路徑，避免競速。
9. **版本單一來源**：`app.js` 驅動全版版本同步；不要手動只改畫面角落。

## 9. 曾放棄／暫緩的方案

- **Web Worker 背景解析**：大量檔案若要真正加速，需把 SheetJS 與 parser 搬入 worker，影響匯入核心且風險高；目前只以進度、分段讓出主執行緒與背景分頁 timeout fallback 改善體感，未做 worker。
- **三套交通系統整併／智慧分析平台**：使用者討論後決定目前維持旅行速率、全日交通量、路口轉向三套獨立系統。現階段不要為未來整併重構；只避免不必要硬編碼並維持可移植性。
- **伺服器多人共用資料庫**：目前不做。Manager 以專案包交換資料。
- **GPT Site**：已刪除且不再維護，除非未來明確重建。
- **第二條 GitHub Pages deploy workflow**：曾有競速風險，禁止重建。

## 10. 重要歷史 Bug 與 Regression 警戒

以下是維護時最容易重犯的類型；詳細演進可查 Git history、README 與歷版驗證檔。

| 問題現象／Root cause | 修正與不可回退事項 |
|---|---|
| 延滯缺值時向下掃到下一個延滯欄，重複計算且靜默成功 | 數值搜索遇下一個非空文字即停止；路段／交叉口延滯缺一即失敗，不能補 0 |
| 旅行或行駛速率讀到相鄰方向／數字標題 | 限定記錄區塊與標籤後有效數值；加入旅行 ≤ 行駛的物理檢核 |
| 工作表多出雙向平均區塊，parser 猜兩筆方向 | 必須恰好兩方向；歧義報錯，不猜、不平均 |
| 代表旅行速率、行駛速率、延滯來自不同筆 | `rebuild()` 只排序一次並整筆 spread 選中紀錄 |
| 缺速限被強制轉數字 0，LOS 變 F | 無有效 ratio 時 LOS=`?` |
| 速限版本只在重建使用，預覽顯示另一個 LOS | 預覽也查當期 `speedVersionFor()` |
| 重新匯入另一季把人工速限蓋回 50 | 只有原本無速限時才採預設值；版本與確認狀態保留 |
| 名稱全半形／符號／日期尾碼形成幽靈路段 | 正規化、別名、疑似比對、日期尾碼清理、路段有效期及合併預覽 |
| 方向名稱只在部分頁面套用，Manager／CSV／結論仍顯示方向1/2 | 全站使用 `displayDirectionName()`；Manager 專案包攜帶 `roadMeta`；舊 Manager 包需重新匯出／匯入才更新 |
| Manager 被誤認為自動同步 Project | 維持快照設計，加入 fingerprint 過期提醒，不自動同步 |
| Project 包漏掉自訂 LOS、三段規則或結論範本 | 完整隨包匯出／還原；還原不得把合法自訂門檻當舊預設刪除 |
| 期別混用民國／西元造成重複季度或排序錯誤 | 內部一律民國、顯示另轉換；嚴格驗證年份／Q1–Q4 |
| 表頭與檔名日別衝突造成平日被覆蓋、假日消失 | 工作表優先但強制警告；同批鍵碰撞擋下 |
| 調查日期不同 sheet 不一致或日期不屬所選季度 | 掃描並顯示來源／衝突／季度警告；日期不參與交通計算 |
| 重匯失敗後仍可按舊預覽確認 | 失敗清除 pending；計畫／季度 context 被鎖定並驗證 |
| IndexedDB 讀取錯誤後用空白 state 覆蓋原資料 | `loadError` 鎖住 save，提供搶救畫面；blocked storage 與 corrupt data 分流 |
| 指定低 DB version 遇到使用者較高版本資料庫即失敗 | `indexedDB.open(DB)` 不硬指定版本 |
| 外部文字直接進 HTML | 全部經 `esc`／`safe`；尤其 Manager 計畫名與說明文字 |
| CSV 依第一列欄位造成來源追溯欄遺失 | 匯出採欄位聯集；方向輸出用顯示名 |
| Excel OOXML 元素順序錯，舊 Excel 修復並刪圖 | 維持 `excel-export.js` 已驗證順序，跑 `ooxml-check.mjs` 與舊版 Excel 實開 |
| 趨勢圖、說明、Excel 各自重算或季度以索引對齊 | 全部共用 `trend.js`；Excel 用季度聯集與 period 精確對位，缺季 null |
| 平／假日加總、缺值補零、未知 LOS 稀釋比例 | 日別分離；缺值斷線；比例分母只含已分級紀錄並顯示未知數 |
| 多張三段圖 ZIP 下載被後段舊 handler 覆蓋、互動重複綁定 | v2.20.54 移除覆蓋／重綁；下載與點擊下鑽需回歸 |
| 趨勢點下鑽只有模糊文字篩選，混入其他季度／日別／計畫 | v2.20.54 改成精確欄位條件；單一計畫及 Manager 都要測 |
| Manager 重匯後保留已不存在的日別篩選 | 自動回到平日＋假日 |

## 11. 已知限制、未解事項與待確認

### 已知限制（不是目前已確認 Bug）

- 資料容量沒有「計畫數無上限」的工程保證；實際上受瀏覽器 IndexedDB quota、裝置磁碟、記憶體與一次匯入大小限制。應定期備份並避免把原始 Excel 放進 Repository。
- GitHub Pages 是公開靜態網站，但 Project 資料仍只在使用者瀏覽器本機；不是多人即時資料庫。
- 目前沒有伺服器同步、帳號權限、衝突合併或稽核後端。
- 目前 parser 專門處理既定旅行速率報表；不是任意格式 Excel 的通用 AI 辨識器。
- 匿名 fixtures 可驗證契約與流程，但不能涵蓋所有業務報表版型。
- 舊資料若在早期版本匯入而未保存 `surveyDate`／來源儲存格，系統無法憑空補出；需重新匯入原始檔才有完整追溯。

### 目前未發現的項目

- v2.20.54 驗證報告沒有列出尚未修復的正式功能 Bug。
- 本次交接未發現新的程式缺陷，且依任務限制沒有修改程式。

### 待確認／尚未在本次重新驗證

- 本次交接沒有重新拿公司真實 Excel 樣本跑一遍輸入→解析→計算→顯示→匯出；v2.20.54 的乾淨驗證使用匿名 fixtures。歷史版本曾做真實樣本檢核，但不能宣稱等同 v2.20.54 本次實測。
- 本次沒有人工用使用者公司的舊版 Microsoft Excel 開啟新生成的輸出檔；v2.20.54 交付前的手冊有用 Word 轉 PDF 視覺檢查，Excel 相容主要由 OOXML／E2E 回歸覆蓋。
- 未知未來瀏覽器 quota、GitHub Pages 政策或第三方套件安全狀態；每次發布前需重新驗證。

## 12. 技術環境、依賴、測試與發布

### 12.1 Node／package manager／dependency

- CI：Node.js 22。
- 正式依賴鎖：`package-lock.json`；CI 使用 `npm ci`。
- `package.json` devDependencies：`docx 9.7.1`、`jszip 3.10.1`、`playwright 1.62.1`、本機 `vendor/xlsx-0.20.3.tgz`。
- 此 Repository 是 npm 專案；不要提交不必要的 pnpm lock 或 node_modules。
- 若本機工具包只有 pnpm，可由 `package-lock.json` 轉出安裝鎖資訊供暫時驗證，但交付與 CI 仍以已提交的 npm lock 為準。
- 無 TypeScript、無 lint script、無 build script；不能把「不適用」寫成「已通過」。

### 12.2 驗證指令與門檻

```text
npm ci
npm test
npm run e2e
node check-version.mjs
```

- `npm test`：所有根目錄 `*.test.mjs`，再跑 `check-version.mjs`。
- `npm run e2e`：先產生 6 份匿名測資，再依序跑 19 支 Playwright E2E。
- 解析／交通計算變更必須額外驗證：輸入 → Parser → 驗證 → details → summary → UI → CSV／Excel／Project package／Manager。
- OOXML 變更需跑結構檢查，並盡可能用實際舊版 Excel 開啟。
- 手冊變更需重建 PDF／DOCX、檢查副本一致、頁面渲染及版本；`manuals/` 只留當版一組。
- 版本更新需執行 `check-version.mjs`，確保 `app.js`、HTML、資產 query、README、手冊、驗證檔與歷史規則一致。
- 正式發布前要在乾淨解壓／乾淨 clone 上安裝鎖定依賴並重跑必要測試，避免工作目錄殘留掩蓋問題。

### 12.3 最後一次已實際完成的正式驗證

對應版本 `v2.20.54`、功能 commit `54a4624ae08c570007cf0f888dd7435e6ffc22c1`：

- 風險：趨勢、Manager、圖表互動與 Excel 匯出跨模組，採全面複查。
- 候選版單元／回歸：161 項，159 通過、0 失敗、2 項依設計略過。
- 候選版 E2E：19／19 通過。
- 乾淨封包：68 個程式檔；依鎖定版本安裝後，單元／回歸同為 159 通過、0 失敗、2 略過，E2E 19／19。
- 手冊：DOCX 經 Word 轉 PDF 檢視 19 頁；PDF 檢視 26 頁，未見裁切、重疊、缺字或版號錯誤。
- production build／TypeScript／lint：不適用，因專案沒有這些腳本。
- 正式發布時 GitHub Actions 測試及 Pages 均成功，線上核心資產 hash 與 Repository 相符，新手冊可下載、舊版手冊 URL 為 404。

本次 2026-09-13 交接只再次執行 `node check-version.mjs`（通過）及 Git／路徑／remote／branch／版本核對；沒有把已完成的 v2.20.54 全套測試重跑冒充新結果。

## 13. 固定 Claude ↔ GPT 開發流程

每次維護固定流程：

Claude 修改／修復  
→ Claude 提供完整檔案與修改說明  
→ GPT 獨立進行風險導向複查  
→ GPT 發現問題時直接修正  
→ 執行必要且完整的 regression／test／TypeScript／lint／build（專案有什麼就跑什麼；不適用需明示）  
→ 全部必要驗證通過後發布 GitHub／GitHub Pages  
→ GPT 提供完整修改、版本、commit、測試結果與高風險資訊  
→ Claude Opus High 再做第二次獨立複查。

GPT 不得因 Claude 宣稱「已完成／已測試」或只改某些檔案，就省略獨立驗證。Claude 的說明是審查線索，不是測試證據。每次候選包應隔離於舊版，先驗 SHA-256／檔案完整性，再從正確基準檢查；不得混入上一候選包資產。

## 14. 完整風險導向複查規則

### 14.1 風險分級

**低風險**：針對本次修改、直接影響範圍及必要的間接影響進行增量複查。

**中風險**：複查所有受影響模組，並追蹤完整相關資料流及跨檔案依賴。

**高風險**：進行全專案層級複查。核心交通工程公式、共用資料結構、Parser／匯入架構、核心計算流程、大型重構、重大跨模組修改，以及可能廣泛影響既有結果的共用邏輯，原則上列為高風險。

### 14.2 所有風險等級共同要求

1. GPT 不受 Claude 宣稱的修改範圍限制。
2. 必須先完整理解 Claude 本次修改目的與修改說明。
3. 優先檢查本次修改、直接影響、間接影響及跨檔案依賴。
4. 發現可疑邏輯、既有 Bug、regression、edge case 或非預期影響時，主動擴大複查。
5. Claude 的「已修正／已測試／正常」只能作為資訊，不能作為驗證證據；GPT 必須獨立確認。
6. 必須驗證必要完整資料流：輸入 → Parser → 驗證 → 資料結構 → 計算 → UI → 匯出。
7. 執行專案目前具備的完整 automated tests／regression／E2E／TypeScript／lint／production build 等必要驗證；不存在的項目明列不適用，不得虛報通過。
8. 發現缺陷時，若使用者已授權「有問題直接修正並發布」，可直接修正；若只是 inspection／報告，先回報缺陷、影響、修正方案及 regression 風險，等確認。
9. 修正後重新執行相關測試及必要完整 regression。
10. 所有必要驗證通過後才能發布。
11. 發布後必須確認 push、Actions、Pages、線上版本／核心資產 hash、新 URL 與預期舊 URL；不能只看到 `git push` 成功就宣稱發布完成。
12. 發布後提供 Claude 二次複查的完整資訊：Claude 原始修改、GPT 發現問題、Root cause、GPT 額外修正、測試／Build、正式版本、commit、發布證據、高風險項目。

不得因更換 GPT 對話、對話過長或額度中斷而簡化。使用者說「複查」或「依風險導向複查規則」時，除非另有指定，均代表執行本節完整流程。記憶不完整、規則衝突或語意不清時先詢問，不自行改成較簡單的檢查。

## 15. 發布與交付規則

1. 先確認正確 Repository、branch、origin、工作目錄與基準版本；有疑問停止 Git 操作。
2. 任何正式候選都先在隔離目錄解壓，驗 SHA-256 與說明；不要覆蓋舊候選或把舊雜湊資產帶入。
3. 保留使用者既有修改，先看 `git status`／diff；禁止以 destructive reset 清掉未知變更。
4. 風險分級、審查、修正與完整驗證後才調版本；若 GPT 有額外程式修正，版號必須推進，避免與 Claude 原包混淆。
5. 提交前檢查 diff、資產引用、手冊版本、`manuals/` 舊檔、驗證報告及 package lock。
6. push `main` 後等待「建置與測試」及 `pages-build-deployment` 成功。
7. 線上核對 `index.html` 顯示版號、核心資產實際 hash、手冊 URL；預期被移除的舊手冊／舊雜湊資產應 404。
8. 完整交付資料夾使用：`D:\Users\95108\Downloads\交通系統交付_YYYYMMDD\交通服務水準_<版本>\`。同日同版修正版加 `_第2次`，不覆蓋或自動刪除先前交付。
9. 交付至少含最新完整程式 ZIP、Pages 包（若該版流程需要）、驗證報告、SHA-256 清單與給 Claude 的獨立交接說明。給 Claude 的說明不可混在正式程式包內，除非既有交付規格明確要求。
10. 不得把原始交通調查 Excel、正式 Project 資料、Manager 私有備份、瀏覽器資料庫或任何敏感資料上傳公開 Repository。

## 16. 長期開發禁止事項與已授權決策

- 不為未來三系統整併而重構本系統；維持模組化與可移植性即可。
- 不改已確認交通計算、LOS 與代表紀錄規則，除非使用者明確提供新方法並要求變更。
- 不把缺資料補 0、不猜方向／標籤、不把未知 LOS 判 F。
- 不混合平日與假日；不把跨計畫路段總數直接相加成可比較總量。
- 不讓顯示名稱變成資料鍵；`方向1／方向2` 永遠是 canonical key。
- 不讓 Manager 自動同步 Project；更新靠重新匯出／匯入，並提供過期提示。
- 不加入 `.openai/hosting.json`；不回報或發布已刪除的 GPT Site。
- 不建立第二條 Pages deploy workflow。
- 不只依 README、舊聊天、Claude 聲明或手動瀏覽就宣稱通過。
- 不把匿名 fixture 測試說成真實業務檔案測試。
- 不覆蓋同日交付，不自動刪除先前備份。
- 回答與審查以事實、證據、正確性為先；使用者假設若與驗證衝突，重新驗證後要明確指出。

## 17. AI 對話換代交接規則

### 17.1 舊 GPT

1. 依本文件及當代完整重要對話，以**實際 Repository** 為基準更新同一份 `PROJECT_HANDOFF.md`。
2. 刪除／修正過時資訊，補入本代新增的重要技術決策、Bug、regression、功能、測試、版本、部署、已知問題與待辦。
3. 假設新 GPT 完全看不到舊對話，進行交接完整性自驗。
4. 重新核對並回報正確交通程式、本機資料夾名稱、完整路徑、origin、branch、版本與 commit，不能照抄舊路徑。
5. 檢查 diff，只提交本文件及明確屬於交接的變更，commit 並 push。

### 17.2 新 GPT

1. 先用使用者提供的完整資料夾核對 `.git`、origin、branch、版本與 Repository 身分。
2. 完整讀取本文件，再核對程式碼、README、package、Git history、測試、Actions 與 Pages；不要只信本文件。
3. 完成正式交接驗收，列出相符、差異、待確認與目前最合理下一步。
4. **未經使用者明確確認「交接確認完成」以前，不得修改程式、commit、push、發布或開始待辦。** 唯一例外是使用者在同一指令中明確授權特定交接修正。
5. 後續永遠更新同一份 `PROJECT_HANDOFF.md`；不建立編號副本。

## 18. 交接完整性自我驗收

本文件已針對下列風險逐項覆蓋：

- 可由完整路徑、origin 與 Repository 名稱避免選錯三套交通程式。
- 已保存目前 LOS、代表紀錄、延滯、趨勢與日別規則，避免用過時公式。
- 已保存 Parser、資料結構、Project／Manager、方向名稱、速限版本與匯出契約。
- 已列出高風險歷史 Bug、Root cause 類型與 regression 防線。
- 已完整保存 Claude/GPT 分工與風險導向複查規則，而非只寫一句名稱。
- 已區分 v2.20.54 實際通過證據、本次只跑的檢查、歷史證據與尚未驗證事項。
- 已保存 GitHub Pages 單一路徑、無 hosting.json、無 GPT Site、交付與備份規則。
- 已保存 AI 換代驗收及「新 GPT 未獲確認不得動工」規則。

## 19. 新 GPT 接手後的合理下一步

1. 使用本文件第 1 節的完整路徑打開 Repository。
2. 執行只讀核對：`git status`、`git remote -v`、`git branch --show-current`、`git log -1`、版本檢查。
3. 完整讀取本文件與當版 `VALIDATION_v2.20.54.md`，抽查 `app.js` 核心規則和 `.github/workflows/ci.yml`。
4. 查 GitHub Actions、Pages 與線上 `https://baotoast.github.io/Travel-speed/` 是否仍對應 v2.20.54。
5. 向使用者回報交接驗收；等待使用者明確回覆「交接確認完成」。
6. 確認後才處理下一份 Claude 候選包或新需求，依第 14 節決定風險並執行。

目前沒有已授權的新功能待辦；正式交接完成後應停止，不自行開始重構或開發。
