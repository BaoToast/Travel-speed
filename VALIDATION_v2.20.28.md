# 交通服務水準 v2.20.28 驗證報告

- 驗證日期：2026-09-01
- 發布目標：`BaoToast/Travel-speed` GitHub Pages
- 前一正式版：v2.20.26（commit `db05db0ed5877b9b72437eb48ef44e00f3b888cf`）
- 複查來源：使用者提供的 v2.20.27 候選包
- 候選 ZIP SHA-256：`9B02684E91DCB9905574612ABF64EB0BFE166F429CF8786EC155C67E3CB4DC5D`，與附件核對檔相符

## 本版功能修正

「計畫設定」原有下拉只把某個計畫載入表單，不會立即切換 `state.activeCode`，但標籤寫成「目前計畫」。當頁首作用中計畫為 A、表單下拉改選 B 且尚未儲存時，使用者容易誤以為匯入會寫入 B；實際上匯入、明細、彙總與圖表仍依頁首作用中計畫 A。

本版：

- 把標籤改為「要編輯的計畫」。
- 表單計畫與作用中計畫不同時，同時指名兩者並說明切換方式；一致時不顯示提示。
- 保留原本資料規則：頁首選單直接切換作用中計畫；按下「儲存計畫設定」才會把作用中計畫切換為表單計畫。
- 提示使用既有共用警示樣式，另加 `role="status"` 與 `aria-live="polite"`；沒有新增零散的行內排版參數。

## 複查候選包後的補正

1. v2.20.27 候選包的驗證報告仍把前一正式版寫為 v2.20.24、複查來源寫為 v2.20.25，測試數、手冊版號與 Pages 查核也停留在 v2.20.26。v2.20.28 已重寫為本次實際來源與結果。
2. 候選交接說明寫「測試 112/112」，實際 Node 測試為 112 項、110 通過、2 略過；兩項略過均依賴附件未提供的真實調查檔。本報告分開呈現，不把略過算成通過。
3. 候選的作用中計畫端對端測試只要求 A 大於 0 筆；v2.20.28 改為嚴格確認 A 恰好 4 筆、B 為 0 筆，且測資缺少時直接失敗，不再以成功狀態略過。
4. 候選 ZIP 內含交接說明；正式備份包會依維護規則把程式與 Claude 說明分開交付。

## 計算與資料界線

本版沒有變更：

- 旅行速率、行駛速率及延滯解析或計算；
- LOS 門檻：A ≥ 0.80、B ≥ 0.60、C ≥ 0.50、D ≥ 0.40、E ≥ 0.20、F < 0.20；
- 代表紀錄順序：先取最差 LOS，再取最低速限比，再取最低旅行速率；
- 旅行速率、行駛速率及總延滯必須來自同一筆代表紀錄；
- 各計畫資料、自訂 LOS 門檻、備份還原與 Manager 資料彼此隔離。

## 本機驗證結果

- Node 測試：112 項，110 通過、0 失敗、2 略過。兩項略過均需附件未提供的真實平假日調查檔，未被誤列為通過。
- 瀏覽器端對端測試：7 支腳本，共 238 個成功檢查、0 個失敗；包含旅行速率、結論草稿、方向名稱、版面、調查日期、年份顯示及作用中計畫隔離。
- 版本一致性：程式、HTML 快取參數、README、更新說明、驗證報告與兩種手冊均為 v2.20.28。
- 手冊：PDF 36 頁、DOCX 25 頁；逐頁轉圖檢查，未發現裁切、重疊或缺字。環境缺少 LibreOffice，DOCX 改以本機 Microsoft Word 匯出 PDF 後完成視覺檢查。
- `git diff --check`：通過。
- 計算核心檔案 `period-date.js`、`quality-extension.js`、`excel-export.js`、`conclusion.js` 均未變更；LOS 與代表紀錄規則另由自動測試鎖定。

本次附件沒有提供真實交通調查 Excel，因此沒有宣稱完成真實原始檔逐筆對帳；驗證範圍為程式規則、匿名測資、瀏覽器操作與既有回歸測試。

## GitHub Pages 發布查核

完成推送及線上逐檔查核後填入。
