# 交通服務水準 v2.20.24 驗證報告

- 驗證日期：2026-09-01
- 發布目標：`BaoToast/Travel-speed` 的 GitHub Pages
- 前一正式版：v2.20.21（commit `3160a0543ff616ef984e41587df2f8f765ad0f76`）
- 本報告涵蓋 v2.20.22～v2.20.23 的候選版內容，以及 GPT 複查後的 v2.20.24 顯示與排序補強

## 本版性質

v2.20.23 候選版記錄了三支系統跨系統徹查的結果。本次 GPT 複查所收到的壓縮包未包含該 37 份真實調查表，因此下列真實檔比對屬於 Claude 提供的既有紀錄；GPT 本次可獨立驗證的範圍是程式碼、匿名測資、單元測試、端對端操作、手冊與公開網站。

**未變更任何計算規則**：旅行速率、行駛速率、延滯加總、LOS 門檻（A ≥ 0.80、B ≥ 0.60、C ≥ 0.50、D ≥ 0.40、E ≥ 0.20、F < 0.20）與代表紀錄選取順序全部不動。

## 修正項目

| 代號 | 嚴重度 | 項目 |
|---|---|---|
| H5 | 高 | 合併路段時速限版本無重疊檢查，未參與合併的紀錄 LOS 被改掉 |
| M2 | 中 | `roadSignature()` 未清除 ASCII 連字號，重複路段偵測對最常見的情形失效 |
| M3 | 中 | 同一份檔案有兩個矛盾的調查日期時仍回報「相符」 |
| M1 | 中 | 三支共用的非調查日期清單漂移 |

### H5 詳述

`applyRoadChange()` 與日期尾碼合併兩處都直接 `concat` 來源路段的速限版本，沒有任何重疊檢查。`speedFor()` 挑版本的規則是「開始季度較新者優先，相同時取後建立的 id」，因此來源路段的版本只要開始季度不早於目標路段的，就會蓋過目標路段**原本就有、且不在本次合併範圍內**的紀錄。實測：目標路段一筆紀錄速限由 40 變成 70、速限比 0.8250 → 0.4714、**LOS 由 A 變成 D**，而確認對話框的 `roadImpact()` 只統計來源路段的列數，完全不會提到。手動新增版本時 `saveSpeedVersion()` 對重疊是會跳確認的，這條路徑不該更寬鬆。

本版新增 `mergeSpeedVersions()`：目標路段既有版本優先，只帶進完全不重疊的，被略過的回報給呼叫端並寫進完成訊息。

### M2 詳述

`roadSignature()` 的字元類列出了各種連字號的**原形**（`‐‑‒–—―－`）與波浪號（`~～〜`），但 `stripRoadSuffix()` 內部已先呼叫 `normalize()`，而 `normalize()` 把那六種連字號全部換成 **ASCII `-`**——它不在字元類裡。於是連字號永遠留著、波浪號永遠被清掉，任何「波浪號 vs 連字號」的配對都算不出相同簽章。

實測 `14013TS601` 平日與假日兩份真實檔的站名分別是「鳳北路(南星路～沿海四路)」與「鳳北路(南星路-沿海四路)」，僅差此一字元；匯入 14 份真實檔得到 **9 條路段而非 8 條**，且 `inspectHealth()` 的「名稱疑似重複」為 0——那個檢查的訊息本身就寫著「標點或分隔符不同，請確認是否為同一路段」。

### M3 詳述

`findSurveyDate()` 取第一個有標示的日期就回傳。實測 `11535TS1501`／`1502`／`1503` 三份真實檔各有兩個有標示的調查日期，分屬 115Q1 與 114Q4（上午尖峰改了、下午尖峰忘了改）。舊版對這樣的檔案回報 `status: "match"`、`「調查日期與所選期別相符」`。本系統對路段名稱（`roadConflict`）與平假日（`dayConflict`）都已經會回報來源衝突，日期是唯一漏掉的一項。本版比照辦理，於預覽列出兩個日期與其工作表／儲存格位置，**仍不阻擋匯入**。

## v2.20.23 追加：期別可以填民國年或西元年，畫面與匯出可以切換兩種寫法

| 代號 | 嚴重度 | 項目 |
|---|---|---|
| Y1 | 中 | 匯入的年份欄位只收民國年，拿到西元年標示的委託案只能自行換算 |
| Y2 | 中 | 期別排序鍵只認 2～3 碼，四碼西元寫法回 -1 會整段排到最前面 |
| Y3 | 低 | 更新說明的「如何更新」段落自 v2.20.20 起未同步版號 |
| — | 新功能 | 頁首「年份顯示」切換鈕：民國年 ⇄ 西元年，畫面與匯出一起換 |

### 切換是純顯示——這一版最需要被證明的一件事

年份切換只換看到的字。分組、排序、識別鍵、期別篩選與所有計算一律走**儲存值**（民國年）。三處實作上的關鍵：

1. **下拉選單一律 `value=` 儲存值，只換顯示文字。** 成果交付範圍與結論草稿的單季條件是 `row.period === scope.quarter` 直接比字串，值一旦跟著換成西元年，畫面立刻變成「所選條件沒有對應的資料」。以天真實作（`<option>` 沒有 `value`，文字即值）實測，結論草稿由「符合條件 12 筆」變成 **0 筆**——端對端測試會抓到。
2. **`deliveryRange().label` 維持儲存值**（它是報告文字草稿的儲存鍵），另加 `displayLabel` 給人看。跟著換的話，切一次年份就找不到之前存的草稿。
3. **`excel-export.js` 的 `periodOrder()` 仍吃儲存值**，圖表類別軸與儲存格都取同一個 `label`，兩邊不會寫出不同的期別。

### 刻意不跟著切換的三處

- 匯入預覽與期別確認視窗裡的期別字樣——它們講的是「會存成什麼」，本來就該是民國年。
- 備份、Project 專案包與成果包內的 JSON 資料檔（會被再匯入的）。
- 成果交付草稿的內部儲存鍵。

## v2.20.24 複查修正

| 代號 | 嚴重度 | 項目 |
|---|---|---|
| Y4 | 中 | `app.js` 與 `excel-export.js` 的季度排序仍只接受 2～3 碼，四碼西元季度會回傳 -1 |
| Y5 | 低 | 路段合併預覽、復原提示、健康檢查與結論草稿仍有少數位置直接顯示民國年儲存值 |
| T1 | 低 | 民國 99 年的西元轉換測試誤把預期值寫成 99Q2，導致該邊界實際被跳過 |

本版將三個排序層（主程式、品質擴充、Excel 匯出）統一為 2～4 碼季度格式；四碼西元年先換算成民國年再產生排序鍵。所有新增修改仍限於顯示與排序，不更動任何速率、延滯或 LOS 計算。

## 驗證結果

- 單元／一致性測試：共 **109 項，107 通過、0 失敗、2 略過**。兩項略過均明確依賴本次附件未附的真實調查檔；相同行為已有匿名與程式層測試保護，但不得宣稱完成真實檔複驗。
- v2.20.24 新增跨層守門：主程式、品質擴充與 Excel 匯出三個季度排序鍵，都必須把 `115Q1` 與 `2026Q1` 視為同一季；另補上民國 99 年轉西元 2010 年邊界。
- 端對端：六組瀏覽器操作測試全部通過、0 失敗；覆蓋匯入與 Excel、結論草稿、方向名稱與 Manager、15 個分頁及 9 種寬度、日期判讀、民國／西元顯示切換。
- 版號、快取參數、手冊檔名、封面版號與**部署說明版號**：`check-version.mjs` 全部通過（新增的部署說明檢查已證明對 v2.20.21 會紅字）。
- 手冊已**重新產生**（非改檔名）：PDF 35 頁、Word 25 頁，內容版號、更新章節與頁尾均為 v2.20.24／2026-09-01；兩份均逐頁轉圖檢查，未見截斷、重疊或破版。
- **Claude 原交接紀錄**：37 份真實檔（含本系統格式 14 份）以 `parse-harness.mjs` 比對 v2.20.21 時輸出 MD5 相同。因本次附件未附原始調查檔，GPT 沒有把這項列為本次獨立驗證結果。
- 三支共用的 `period-date` 模組已同步，四種非調查日期在三支的判斷完全一致。

## 結論

v2.20.24 已補齊候選版的西元季度排序與顯示同步缺口；計算規則未變更，現有黃金值、匿名測資、完整單元測試、瀏覽器操作與手冊檢查均通過，可發布至 GitHub Pages。
