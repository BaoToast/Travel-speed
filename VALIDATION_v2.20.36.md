# 交通服務水準 v2.20.36 驗證報告

- 驗證日期：2026-09-05
- 複查輸入：Claude 交付的 v2.20.35 完整專案
- 前一正式版：v2.20.33（commit `2df77331ddd531b97c82e3f78381e66c49dad41b`）
- 本版性質：拖曳上傳與批次讀取狀態修正；沒有變更任何計算或資料規則。

## 輸入檔案完整性

使用交付資料夾內的 `SHA256SUMS.txt` 核對兩份 ZIP，完整專案與複查說明的 SHA-256 均與清單相符。

## 複查發現與修正

### 一、拖曳亮起狀態可能提早消失

v2.20.35 以 `dragleave` 的事件目標判斷是否離開放置區。游標從放置區背景移到區內文字等子元素時，瀏覽器也可能送出 `dragleave`，使亮起狀態提早熄滅。

v2.20.36 改以進入／離開深度計數，只在游標真正離開整個放置區時清除亮起狀態；放下檔案時會重設計數。

### 二、批次讀取中仍可換入另一批檔案

v2.20.35 讀取期間雖已停用「讀取並預覽」按鈕，但檔案欄仍可點選或拖入另一批檔案。這不會改變正在執行的本批區域變數，卻可能讓畫面上的檔名與份數變成新批次，與實際判讀內容不一致。

v2.20.36 在判讀期間暫時停用檔案欄，拖入時也會明確提示等待本批完成；讀取結束後自動恢復。

### 三、進度測試未接入完整端對端指令

v2.20.35 已有 `e2e-progress.mjs`，但 `package.json` 的完整端對端指令未包含它。v2.20.36 已補入，避免日後完整驗證漏掉進度功能。

## 守門案例

- 拖曳游標在放置區內移過子元素時，亮起狀態持續；真正離開後才熄滅。
- 多檔判讀每一個進度狀態中，檔案欄都維持鎖定；完成後恢復可選檔。
- 尖峰 Excel、備份 JSON、Manager JSON 的拖曳匯入與副檔名拒絕維持通過。
- 放置區外的檔案放下事件維持被阻擋。

## 計算與資料邊界

本版沒有修改旅行速率、行駛速率、路段延滯、交叉口延滯、總延滯、LOS 門檻、代表紀錄選取順序、日期解析、路段名稱正規化、方向名稱同步、備份還原內容或計畫隔離規則。解析與正式寫入的計算流程未變更。

## 驗證結果

- 單元／回歸：共 121 項，119 通過、0 失敗、2 項條件略過。略過的是需要額外真實工作簿的日期案例；沒有把略過誤報成通過。
- 版號一致性：全部通過，程式、快取參數、手冊與驗證報告均為 v2.20.36。
- 端對端瀏覽器：12 支腳本全部通過，包含計算、結論、方向名稱、版面、日期、年份顯示、計畫隔離、路段有效期間、結果定位、拖曳、進度及表頭篩選。
- 手冊：PDF 21 頁與 Word 18 頁均重新產生並逐頁檢查，未見裁切、重疊、空白頁或版本錯置。
- 程式差異檢查：`period-date.js`、`column-filter.js`、`conclusion.js`、`excel-export.js`、`quality-extension.js` 相對前一正式版無差異。

乾淨備份包重驗、GitHub Actions 與公開 GitHub Pages 核對會在發布階段完成，結果記入獨立的 Claude 複查說明。

## 證據界線

本輪沒有取得額外的真實 Project／Manager 工作簿，因此不宣稱由真實工作簿重新驗證計算結果。結論以程式碼差異、固定樣本、自動化測試與瀏覽器端對端測試為依據。
