# 交通服務水準分析系統 v2.20.16 驗證報告

## v2.20.16 複查新增項目

- 不同 Project 同一季度的調查月份互不混用。
- Manager 表格、LOS 圖表與旅行速率圖表的月份均取自所選計畫的匯入包。
- 畫面顯示的月份文字可由 Project 與 Manager 搜尋框找到。
- 只有 `surveyDate` 更新時，Manager 會提示專案包已過期。
- 僅 Manager 專案包具有日期時，期別顯示切換仍可使用。
- 未更動服務水準門檻、代表紀錄選取、速率、延滯或 LOS 計算。

> 版號說明：原本規劃的 v2.20.14 從未發布，內容已完整併入本版。
> 本版另補上「日期不再接受不存在的曆日」（來自路口轉向的複查，共用檔同步）。

日期：2026-08-30

## 一、本版做什麼

兩件事，**都沒有變更服務水準門檻、代表紀錄選取、總延滯計算、匯入方式或備份格式**。

### 1. 調查日期 × 季度一致性檢查

| 情況 | 行為 |
| --- | --- |
| 日期落在所選季度內 | 不打擾，直接寫入 |
| 日期不在所選季度內 | 預覽上方**紅底標示**（哪幾份、檔案裡的日期、屬於哪一季、來源儲存格）；按「確認寫入尖峰明細」時**再跳一次確認框** |
| 表頭讀不到日期 | **不阻擋**。只提醒「本次資料無法辨別日期，所以無法幫忙確認是否符合期別，請自行確認正確性」 |
| 日期沒有「日期：」標示 | 照樣比對，但註明是系統從表頭推測的 |

**不看固定欄位位置。** 各家報表的日期欄擺放都不一樣（實測看過 `上午!AB3`、
`統計表 (1)!B2`），所以新增的 `surveyDateFromWorkbook()` 自己開活頁簿掃每張
工作表的前 12 列，**完全獨立於 `parsePeakSheet()` 與 `matrix()`**——不經過任何
一條讀速率、延滯或 LOS 的路徑，所以不可能改到任何數值。

### 2. 期別顯示切換：季別 ⇄ 實際調查月份

畫面右上角多一顆按鈕。平常顯示 `115Q1`，按一下改成這一季實際做調查的月份
（例：「115年2、3月」）。尖峰明細、尖峰彙總、LOS 圖表與 Manager 比較表一起換。

**只換顯示的文字。** 資料仍以 `period` 分組、排序與計算；`id` 與 `rowKey`
都不含調查日期，覆蓋判斷完全不受影響；匯出的 Excel 內容一個字都沒變。

⚠️ **v2.20.15 以前匯入的資料沒有存調查日期**，那些季度照舊顯示季別；重新匯入
原始檔之後就會有月份。整個計畫都讀不到時切換鈕停用並寫明原因。

---

## 二、三支程式共用同一份判斷

判斷邏輯集中在新檔 `period-date.js`——路口轉向、全日交通量、交通服務水準
**同一份程式、同一份測試表**（`period-date.test.mjs` 的三張表在三支裡逐字相同）。
這一支沒有打包工具，所以是 IIFE 掛 `globalThis.PeriodDate`；另外兩支是 ES module
的 `lib/period-date.ts` / `app/period-date.ts`，內容相同。

---

## 三、守門檢查對未修正版（v2.20.13）的實測

`e2e-period-date.mjs`（20 項），**用真的瀏覽器、同一份現產匿名測資，只換程式版本**。
量的都是兩版都有的畫面文字與寫入結果。

| 檢查 | v2.20.13 | v2.20.16 |
| --- | --- | --- |
| A 日期在季度內時不打擾 | ✅ | ✅ |
| B 預覽上方標示不一致 | ❌ | ✅ |
| B 提示寫出日期 / 屬於哪一季 / 來源儲存格（`上午尖峰!AB3`） | ❌×3 | ✅×3 |
| B 按「確認寫入」跳二次確認框 | ❌ | ✅ |
| B 二次確認按「取消」不寫入 | ❌（**直接寫進去了**） | ✅ |
| C 讀不到日期時的提醒字句 | ❌ | ✅ |
| C 讀不到日期照樣匯得進去 | ✅ | ✅ |
| 有期別顯示切換鈕 / 預設季別 / 可切到月份 / 顯示「115年1、3、8月」 | ❌×4 | ✅×4 |
| 切換前後明細表**每一格**數字逐字相同 | ✅ | ✅ |
| 沒有 JS 例外 | ✅ | ✅ |

→ v2.20.13 實測 **20 項中 11 項紅字**。

「切換前後每一格相同」這一條是逐格比對整張尖峰明細表，只允許第 1 欄（期別）不同，
而且另有一項確認表格真的有資料（不是拿空表當通過）。

---

## 四、測試結果

| 項目 | 結果 |
| --- | --- |
| `npm test`（含新增 `period-date.test.mjs` 10 項＋版號一致性 18 項） | **65/65** |
| `npm run e2e`（5 支腳本，含新增的 `e2e-period-date.mjs`） | **194 項全數通過**，0 JS 例外 |
| 手冊 | v2.20.16 PDF／DOCX 已重新產生，舊版已刪除 |
| 版號一致性 | `index.html` 的 `?v=`、靜態字樣、`app.js` 寫入的版本、手冊檔名與封面戳記、`package.json` 全部 2.20.16（由 `check-version.mjs` 把關） |
| 交付包 | 41 個檔，無 `node_modules`、無 `test-fixtures`（由 `npm run fixtures` 現產）、無調查資料 |

---

## 五、請複查的重點

1. `surveyDateFromWorkbook()` 是否真的沒有碰到任何速率／延滯／LOS 的路徑。
2. `periodLabel()` 是否**只用在顯示**——特別是匯出的 Excel 與 Manager 專案包
   內容應完全不受切換影響。
3. 讀不到日期時**絕不可以**變成阻擋（`status === "unknown"` 必須永遠不跳確認框）。
4. `period-date.test.mjs` 的三張表與另外兩支程式是否仍逐字相同。
5. `index.html` 是否已載入 `period-date.js`，而且**排在 `app.js` 前面**
   （`app.js` 在解析時就會用到 `globalThis.PeriodDate`）。

---

## 六、發布注意

- 上傳只覆蓋不刪除，請**手動刪除** `manuals/` 裡所有**不是 v2.20.16** 的
  手冊 PDF／DOCX。
- 線上版本判斷用 `?v=2.20.16`、帶版號的手冊路徑。
- `period-date.js` 是**新增檔案**，漏傳的話整支程式會在載入時就壞掉，
  請確認它有一起上傳。
