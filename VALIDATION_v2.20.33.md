# 交通服務水準 v2.20.33 驗證報告

- 驗證日期：2026-09-04
- 前一正式版：v2.20.31（commit `d5a5a5d46345101c38ef635254b34f39ff4ac168`）
- 本次基準：使用者提供的 Claude v2.20.32 完整專案

## 複查結論

v2.20.32 新增 Excel 式表頭欄位篩選、按鈕結果自動帶入視野，以及結論草稿介面整理。複查發現並修正一個欄位篩選狀態錯誤，其餘新增功能未發現會阻擋發布的問題。

本版未變更旅行速率、行駛速率、路段延滯、交叉口延滯、總延滯、LOS 門檻、代表紀錄選取順序或計畫隔離規則。

## 本次修正

### 全文搜尋不再誤清除表頭欄位條件

原本先在表頭勾選「115Q2＋A 路段」，再於全文搜尋輸入「B 路段」時，畫面會暫時沒有交集；程式卻把 A 路段誤判成資料已被刪除，直接清掉使用者的欄位條件。清空搜尋框後，原條件無法恢復。

修正後分開使用兩組資料：

- 搜尋後的資料只供畫面結果與漏斗選項使用。
- 目前計畫的完整資料才用於判斷已勾值是否真的因刪除或改名而失效。

尖峰明細、尖峰彙總與 Manager 比較共用相同修正。新增回歸案例驗證無交集時顯示 0 筆、欄位條件仍在，以及清空搜尋後原結果恢復。

## 驗證結果

- SHA-256：兩個使用者交付壓縮檔均與隨附 `SHA256SUMS.txt` 相符。
- 乾淨安裝：完成，套件稽核 0 個已知弱點。
- Node 單元／回歸：121 項，119 通過、0 失敗、2 略過。兩項略過是交付包未附真實調查工作簿，並非測試失敗。
- 端到端瀏覽器驗證：10 支腳本全部通過，涵蓋匯入與數值、結論草稿、方向名稱、版面、多計畫日期隔離、年份顯示、計畫作用域、路段有效期間、結果捲動與欄位篩選。
- 欄位篩選專項：新增的無交集搜尋狀態案例通過；尖峰明細、尖峰彙總與 Manager 表頭篩選均可使用。
- 手冊：重新產生 v2.20.33 PDF 與 Word；PDF 21 頁、Word 18 頁，逐頁視覺檢查未見文字截斷、重疊或缺字。

## 計算與資料邊界

- LOS 預設門檻維持 A ≥ 0.80、B ≥ 0.60、C ≥ 0.50、D ≥ 0.40、E ≥ 0.20、F ＜ 0.20。
- 代表紀錄仍依「最差 LOS → 最低速限比 → 最低旅行速率」選取。
- 旅行速率、行駛速率與總延滯仍整筆取自同一筆代表紀錄。
- 每個計畫的 LOS 門檻、方向名稱、路段資料與備份仍分開保存。
- Manager 仍使用匯入當時的 Project 專案包，不會自動同步本機 Project。

## 證據界線

本次交付包沒有附真實調查工作簿，因此沒有重新執行依賴真實檔案的兩項測試，也不把 Claude 先前的真實檔驗證當成本次獨立證據。以上結論涵蓋本次實際執行的靜態守門、匿名測資、單元測試、瀏覽器操作測試與文件視覺檢查。
