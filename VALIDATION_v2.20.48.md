# 交通服務水準 v2.20.48 驗證報告

- 驗證日期：2026-09-08
- 複查基準：Claude 交付的 v2.20.47（輸入壓縮檔 SHA-256 已符合隨附清單）
- 風險分級：**全面複查**。本次雖未改核心 LOS／代表紀錄計算，但新增跨計畫資料定義、趨勢彙整、圖表呈現與 Excel OOXML 匯出，屬跨模組完整資料流。
- 正式版本：v2.20.48（GPT 複查發現缺陷並修正，故不沿用 v2.20.47 版號）

## 一、Claude 原始修改

- 新增單一計畫六種歷季趨勢指標、圖表講稿、三段服務水準顯示與 PNG 匯出。
- Manager 改為跨計畫歷季趨勢。
- 新增可編輯趨勢 Excel，包含資料、原生圖表與說明三張工作表。
- 新增趨勢、圖表版面與 Excel 匯出測試。

## 二、GPT 複查發現並修正

1. 首季 X 軸標籤與 Y 軸底端刻度重疊；首尾標籤改用向內對齊，版面測試通過。
2. 趨勢 Excel 的 `sheet2.xml` 把 `drawing` 放在 `pageMargins` 前，違反工作表元素順序；改正後 Open XML 驗證為 0 錯誤，Microsoft Excel 16.0 實際開啟成功（3 張工作表、1 張原生圖表）。
3. 平均類指標的樣本／下鑽列與缺值數原本沿用 LOS 是否存在，可能把缺少該指標的紀錄納入說明；改為依所選數值欄位的有效值計算。
4. 頭尾之間完全缺少的季度未建立空點，折線會跨過未調查季；現在補齊季度並以空值斷線。
5. 選「平日＋假日」時，同一路段的兩個日別被誤稱為兩條路段；改稱「路段日別紀錄」。
6. Manager 原本讓各專案包各用自己的 D／E 壅塞門檻，卻畫成同一指標，跨計畫比較定義不一致；新增共同壅塞門檻，僅影響比較圖，不改寫專案資料。
7. 跨計畫講稿把「惡化方向最大」誤稱為「變化最大」；改為分別報告惡化幅度最大與改善幅度最大。
8. 補出的 0 筆空季會遮掉其他季度的小樣本警告；樣本下限改為只比較有資料季度。
9. 平均延滯等數值缺漏時，警告一律誤稱「讀不到速率」；改為依指標顯示有效數值缺漏。
10. Word 手冊頁首與文件作者仍殘留姊妹系統「全日交通量及車種組成」；已改為交通服務水準分析系統。
11. 手冊仍說 PNG 附講稿、趨勢 Excel 不存在、Manager 各用自己的門檻；均依實際程式修正，並移除造成近乎空白頁的不必要分頁。

## 三、核心交通計算邊界

- LOS 門檻仍為 A≥0.80、B≥0.60、C≥0.50、D≥0.40、E≥0.20、F<0.20。
- 代表紀錄仍依「LOS 最差 → 速限比最低 → 旅行速率最低」挑選，旅行速率、行駛速率與總延滯整筆取自同一紀錄。
- `excel-export.js`、`column-filter.js`、`period-date.js`、`quality-extension.js` 與 `los-and-selection-golden.test.mjs` 相較前一正式版未修改。
- 本版修改的是趨勢彙整、比較定義、呈現、匯出及文件，不改尖峰資料解析與核心交通計算。

## 四、驗證結果

最終候選版與重新封裝的乾淨副本均已驗證：

- 單元／回歸／版本一致性：149 項，147 通過、0 失敗、2 項條件略過。兩項略過只需要未隨匿名測資交付的真實調查附件，並非失敗。
- E2E 真實瀏覽器：19／19 支腳本全部通過；涵蓋匯入、重匯、拖曳、日期、方向名稱、年份顯示、儲存封鎖、資料載入失敗保護、趨勢資料、圖表版面及 Excel 匯出。
- 乾淨封包重驗：重新建立獨立目錄、重新安裝相依套件後，以上 149 項測試與 19 支 E2E 再次全部得到相同結果。
- production build：本專案為純靜態 HTML／CSS／JavaScript，沒有打包或 TypeScript／lint 步驟；以靜態引用完整性、版本守門及真實瀏覽器 E2E 代替。
- 趨勢 Excel：結構性 E2E 通過；Open XML 驗證為 0 錯誤；Microsoft Excel 16.0 實際唯讀開啟成功（3 張工作表、1 張原生圖表）。
- 手冊：PDF 24 頁與 Word 17 頁均逐頁渲染檢視；未見截斷、重疊或錯誤系統名稱。
- GitHub commit、Actions／Pages、線上版本與重要檔案雜湊將另記於發布後產生、與完整程式壓縮檔分開保存的 Claude 交接說明。

## 五、仍應由下一位 reviewer 注意

- 多個真實專案、不同季度涵蓋範圍與不同原始三段門檻的 Manager 比較。
- 舊版 Microsoft Excel（2007／2010／2013）實機開啟趨勢活頁簿；本次本機實測版本為 Excel 16.0，另以無 x14／x15 與 Open XML 結構守門補強。
- 極長計畫名稱、40 季以上、缺季與小樣本在不同 Windows 顯示縮放／字型環境的視覺差異。
