# 交通服務水準分析系統 v2.20.21 驗證報告

日期：2026-08-31

正式基底：v2.20.19（commit `30c0135c37a8d9a2140976a36f8102a49e647423`）

候選來源：Claude 提供的 v2.20.20，經 GPT 複查補正共用函式接線、測試覆蓋與文件後升版。

## 結論

Claude 找到的延滯錯抓問題屬實。舊版在某一延滯分項缺值時，可能越過下一個文字標籤，
撿到另一延滯分項的數字並繼續寫入。修正後，兩條延滯解析路徑遇到下一個非空文字格
都會停止，缺值會回傳 `null`，由既有預覽流程明確報錯。

本版沒有變更旅行速率、行駛速率、延滯加總、LOS 門檻、速限比或代表紀錄選取規則。

## Claude v2.20.20 的主要修正

- `delayPart()` 遇到下一個非空文字格時停止，不再向下撿到其他延滯欄位的值。
- `valueBelowLabel()` 套用相同停止規則，保護分塊版型的主要解析路徑。
- `period-date.js` 匯出 `isNonSurveyDateText()`，供不同交通系統共用非調查日期清單。
- 新增延滯缺值回歸測試。

## GPT 複查新增修正

1. **共用函式真正接線**：`dayFromWorkbook()` 改為直接呼叫
   `PeriodDate.isNonSurveyDateText()`，移除第二份重複正規表示式造成的未來漂移風險。
2. **兩條解析路徑都受測**：回歸測試由只驗證 `delayPart()`，擴充為同時驗證
   `delayPart()` 與 `valueBelowLabel()` 的正常數值、`N/A`、`-`、`休`、空白及 `null`。
3. **完整日期清單受測**：新增製表、列印、印製、報告、出圖、填表、核定、審查、
   校核、繪製、修正與更新日期的共同測試，並確認調查／監測日期不會被排除。
4. **交付文件更正**：重寫候選包中內容仍停在 v2.20.19 的驗證報告，並修正 README
   將 v2.20.19 標題誤植為 v2.20.20 的問題。

## 計算邊界確認

- LOS 門檻維持：A ≥ 0.80、B ≥ 0.60、C ≥ 0.50、D ≥ 0.40、E ≥ 0.20、F < 0.20。
- 代表紀錄維持：先取 LOS 最差；同級取速限比最低；仍相同取旅行速率最低。
- 旅行速率、行駛速率、路段延滯、交叉口延滯與總延滯仍取自同一筆尖峰／方向紀錄。
- Project 與 Manager 的計畫隔離、門檻隔離、季度與調查月份顯示規則不變。
- 備份格式與既有欄位不變。

## 驗證結果

| 檢查 | 結果 |
| --- | --- |
| 單元與一致性測試 | 94／94 通過 |
| 完整瀏覽器流程 | 202／202 通過 |
| 版本、快取參數與手冊一致性 | 全部通過 |
| 舊版 `.xls` 匿名測資 | 完整辨識 4 筆尖峰方向 |
| Excel 匯出結構 | 旅行速率與 LOS 活頁簿通過 OOXML 檢查 |
| 多寬度版面 | 640～1920 px 無橫向溢出 |

## 證據範圍

本次收到的程式候選包內含匿名 XLS／XLSX 測資，但沒有附上使用者的實際交通調查原始檔。
因此本報告只宣稱完成匿名測資、程式測試與瀏覽器端到端驗證，不宣稱完成真實檔案逐筆核對。

## 發布限制

- GitHub Pages 為唯一正式發布目標。
- 不新增 `.openai/hosting.json`。
- 不新增與 GitHub 內建 Pages 部署競爭的工作流程。
