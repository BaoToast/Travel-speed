# 交通服務水準 v2.20.38 驗證報告

- 驗證日期：2026-09-05
- 前一正式版：v2.20.36（GPT 發布，commit `ef8302197369cd628fbe72cccc42add418e79ca3`）
- 複查基準：Claude 提供的 v2.20.37（直接修改於 v2.20.36）
- 本版性質：**保留 v2.20.37 修正並補齊錯誤復原。沒有變更任何計算。**

## 本版修正

### 一、全域拖放防呆把一般文字拖曳也擋掉了

v2.20.35 加入「檔案掉在放置區外面不要把使用者帶離頁面」時，少了一層
「拖進來的是不是檔案」的判斷。結果使用者在頁面內拖動選取文字也被擋住。

實測（v2.20.36）：

| 拖曳目標 | 拖一般文字是否被擋 |
|---|---|
| 計畫編號輸入框 | 被擋 |
| 全文搜尋框 | 被擋 |
| 結論草稿文字框 | 被擋 |
| 表格區域 | 被擋 |

改成只有 `dataTransfer.types` 含 `Files` 時才攔截。
全日交通量 v20.47 本來就是這樣寫的，本版與它對齊。

### 二、判讀後的整理階段丟例外時，畫面會永久卡死

迴圈裡每一份檔案都有自己的 `try/catch`，但迴圈**之後**的整理步驟
（`pendingPeriodChecks`、`analyzeRoads`、`renderPreview`）沒有任何保護。

實測（v2.20.36）：在整理階段注入一個例外之後——

- 「讀取並預覽」按鈕 `disabled` 維持 `true`，**永久停用**
- 狀態欄卡在「讀取中… 已完成 2／2 份」
- 使用者只能重新整理頁面

路口轉向 v2.1.48 已經用 `try/finally` 修了同一類問題，這一支當時沒有跟上。
v2.20.37 補上 `try/finally`，按鈕與檔案欄都能恢復；本次複查另發現，
該做法只會解鎖控制項，狀態欄仍可能停在「讀取中」，未完成的 `pending`
結果與確認寫入狀態也可能殘留。v2.20.38 增加完整錯誤復原：清除未完成
預覽、停用寫入與取消、清空檔案選取，並明確告知資料未寫入及如何重試。

## 突變測試（守門測試必須真的會紅）

| 對程式做的破壞 | 結果 |
|---|---|
| 全域防呆改回不判斷是否為檔案 | ❌ 3 項紅字（輸入框、搜尋框、表格區域） |
| 拿掉控制項復原保護 | ❌ 按鈕或檔案欄鎖住 |
| 只解鎖、不清除未完成預覽 | ❌ 狀態仍在讀取中或留下可誤按的寫入狀態 |

另外也重跑並確認 GPT 在 v2.20.36 新增的兩支守門測試仍然有效：

| 對程式做的破壞 | 結果 |
|---|---|
| 拖曳深度計數改回 v2.20.35 的事件目標判斷 | ❌ 「游標移到放置區內文字時高亮不會提早消失」紅字 |
| 判讀中不鎖檔案欄 | ❌ 「判讀期間會鎖住原始檔選取框」紅字 |

## 驗證結果

- 單元／回歸測試：121 項；119 通過、0 失敗、2 略過。
- 略過的 2 項需要交付包未附的真實調查工作簿；其餘匿名、結構與計算
  守門測試均已實際執行。Claude 在備有 37 份真實工作簿的環境中曾驗證
  121 通過、0 略過，兩組結果的差異來自測試資料是否存在。
- 端到端瀏覽器驗證：12 支腳本全部通過；另先執行 1 支匿名測資產生器。
- 版號一致性檢查：全數通過。
- 手冊：重新產生 v2.20.38 PDF（21 頁）與 Word（18 頁），逐頁檢視無
  文字裁切、重疊或缺頁。

> 關於略過數：GPT 的 v2.20.36 報告寫「119 通過、2 略過」，本報告寫
> 「121 通過、0 略過」。兩邊都正確——那兩項需要真實調查工作簿，
> 交付 ZIP 不含 `test-fixtures` 與真實檔，GPT 的環境跑不到；
> 本機備有 37 份真實工作簿，因此**實際執行並通過**。

## 計算與資料邊界

本版沒有修改旅行速率、行駛速率、路段延滯、交叉口延滯、總延滯、LOS 門檻、
代表紀錄選取順序、日期解析、路段名稱正規化、方向名稱同步、備份還原內容
或計畫隔離規則。`parseFile()` 一行都沒有動。

## 證據界線

發布前已完成本機驗證；GitHub Actions 與線上雜湊於發布後另行核對。
