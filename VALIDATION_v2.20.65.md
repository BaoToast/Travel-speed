# 交通服務水準 v2.20.65 驗證報告

## GPT 獨立風險導向複查（2026-09-19）

- 正式基準：`BaoToast/Travel-speed` `main`，複查前 HEAD／`origin/main`／GitHub 遠端 `main` 均為 `58167e0fe63c1de9812660572eabd15b598750f4`，工作樹乾淨。
- 候選來源：Claude 提供的 v2.20.65 完整專案、試用版、手冊、給 GPT 的說明與 SHA-256 清單；五項原始交付雜湊均核對相符。
- 風險分級：**高風險／全專案複查**。雖然使用者已確認 v2.20.54 至 v2.20.65 的版次躍進是多輪試用修正的正常結果，但本版涵蓋 parser、全站主篩選、LOS／速限／三段規則作用域、圖表、匯出、導覽與大量共用資料流，不能只按 Claude 宣稱的檔案範圍抽查。
- GPT 額外發現並修正：
  1. `package-lock.json` 根版本仍為 v2.20.61，與 `package.json` v2.20.65 不一致；已修正並新增鎖檔名稱／版本守門。
  2. `wording.test.mjs` 使用 URL pathname 組 Windows 路徑，造成 `D:\\D:\\...`；改用 `fileURLToPath()`。
  3. PDF 手冊下載表格被拆到單獨一頁；調整列印分頁後，完整表格與後續段落同頁呈現，頁數由 26 頁縮為 25 頁。
  4. 移除三支未被套件流程引用、含特定環境路徑的候選暫存腳本；保留 E2E 使用的 `_toolbar.mjs`。
  5. `e2e` 指令內硬呼叫 `npm run fixtures`，在沒有全域 npm 命令的環境無法開始；改為直接執行同一支 `generate-test-fixtures.mjs`，並讓守門驗證實際目的而非綁死套件工具名稱。
- Node／契約／回歸：共 230 項，**228 passed、0 failed、2 skipped**。兩項 skip 均要求真實成對／雙日期業務檔，未宣稱通過。
- Playwright E2E：先執行 fixtures，56 支依序執行，**56/56 通過**；中斷前 41 支與續跑 15 支均有完成紀錄，沒有並行執行。
- 核心計算核對：預設 LOS A≥0.8、B≥0.6、C≥0.5、D≥0.4、E≥0.2；未知速限為 `?`；代表紀錄依最差 LOS、最低 ratio、最低旅行速率挑選；總延滯任一組成缺值即為 `null`；預設篩選結果與舊資料流一致。
- 版面：以實際單檔試用版、3 季匿名 fixtures，逐頁檢查 18 頁 × 1536×864 與 18 頁 × 1366×768；36 張畫面均無水平溢出、文字重疊、圖表超界或表格裁切，瀏覽器亦無 JavaScript 例外。
- Microsoft Excel：由正式 UI E2E 產生 `e2e-trend-excel.xlsx`，再以本機 Microsoft Excel 唯讀開啟；無損毀／修復錯誤，3 個工作表完整，圖表頁 2 個原生圖表均可由 Excel 匯出且人工檢視正常。
- 手冊：重建為 25 頁 PDF，已逐頁渲染與人工檢視；沒有空白頁、重疊或截斷。
- 真實業務檔：本輪沒有取得新的使用者真實報告檔，因此未重跑 `verify-against-summary.mjs <檔案>`；不得把匿名 fixtures 說成真實業務檔驗證。
- 正式功能 commit：`2888a319032ec7d7bcba59e794ef499157e1bc67`；第一階段交接 HEAD：`18d2e3558e75a48fac01cd13848f12a584b8a6fb`。
- GitHub Actions：建置與測試 run `35410073884` 成功；Pages run `35410073205` 成功，兩者皆對應 `18d2e355...`。
- GitHub Pages：`https://baotoast.github.io/Travel-speed/` 已顯示 v2.20.65。線上 `index.html`、`app.js`、`main-filters.js`、`main-toolbar.js`、`los-rule-scope.js` 與 v2.20.65 手冊 SHA-256 均逐檔等於 Repository；新手冊 HTTP 200，舊 v2.20.54 手冊 URL 為 404。

---

- 驗證日期：2026-09-18
- 驗證基準：大檢查（Fable 5.1）修正版，基於 v2.20.64
- 計算：LOS 門檻＝《2022 年臺灣公路容量手冊》表 16.12（A≥0.8、B≥0.6、C≥0.5、D≥0.4、E≥0.2）逐值核對；
  `verify-against-summary.mjs` 對真實報告延滯統計表 6/6 相符；代表紀錄挑選手算相符。
- 說明句：e2e-filter-coverage 反向斷言全綠（applied 標記後）；plaintext-markup、wording 守門全綠，
  反證：注入 `**x**`／「按下面的」→ 紅；speed-label-scan 在 v2.20.64 上 2 紅（訊息沒有儲存格）。
- check-version.mjs 全部通過；npm test 全綠。

---

## 前一版（v2.20.64）驗證紀錄


- 驗證日期：2026-09-17
- 驗證基準：Claude 工作版 v2.20.60（本輪為使用者實測回饋的修正，尚未經 GPT 複查）
- 風險分級：**側欄行為與版面**，不含任何計算口徑變更

---

## v2.20.64（2026-09-17）本版驗證重點

- **匯入時「重複」要出聲**（X-66）：使用者問三支程式是否都認標籤、
  欄位換位讀不讀得到、重複會不會指出異常。本支的答案是「都做到了」
  （X-65 剛補完最後一個缺口），另外兩支各自補了自己的洞。
- **稽核表 L**：結論草稿裡兩種分母並排——「A 級 n 筆（x%）」用全部筆數、
  「三段分法佔比」用可判定筆數。已統一成可判定筆數，並把口徑寫進句子裡。
  守門不比字串，直接把兩段的百分比各自加起來驗是不是都 100%。
- **稽核表 M**：Excel 匯出的分組鍵撞到時，後者會**無聲蓋掉**前者。
  已改成數值不同就整份擋下匯出並說出是哪幾筆（數值相同不算衝突）。
- 單元測試 257 條全綠。

## v2.20.62（2026-09-17）本版驗證重點

- **四張圖各自一個大分頁**（X-62）：`e2e-nav-sections` 新增一整段反向驗證
  （四頁真的存在、各自畫得出自己那一塊、不列同名小分頁）；
  `e2e-view-isolation` 逐頁確認每一頁只顯示自己（18 頁）。
- **甲案**：`e2e-main-toolbar`／`e2e-filter-coverage` 的還原值與案例已隨之更新，
  並補上「預設狀態下不可以出現條件式的不適用說明」的反面驗證。
- **「已確認，不再提醒」**：`issue-ack.test.mjs`（6 條）守指紋要帶數值、
  只有「人工確認」類可以按、確認後兩個數字都要寫出來。
- **旅行／行駛速率的標籤掃描補一道守門**（X-65，使用者提問時查出來的）：
  舊解法只數「平均總旅行速率」，行駛速率那一邊用「離最近的那一個」靜靜挑，
  同一個方向被誤植成兩筆行駛速率時不會出聲。詳見第五節。
- 全套 e2e（54 支）與單元測試（251 條）全綠。


## 一、點大分頁不會自動展開它的小分頁

### 使用者原話

> 「當我點選 交通服務水準的 大分頁標題(路段管理)時，它並沒有展開而是保持收合，
>   請修正成自動展開下面的各項小分頁，如同另外兩個程式一樣的做法」
> 「全日交通量程式，我點選分類標題(例如二、參數設定)時，並未自動展開下方的
>   大分頁和小分頁，請同步確認三份程式是否都能自動展開」

### 實測到的成因（不是推測）

用全新的瀏覽器設定檔逐頁量過一次：**全新狀態下是展開的**（路段管理 8 項、
路段速限 2 項、尖峰彙總 3 項…），所以不是「一開始就壞」。

真正的成因是**收合狀態被寫進 localStorage**（`los-nav-collapsed-v1`）：
只要按過一次那顆 ▸ 收合鈕，那一頁就**永遠**是收的，
再怎麼點那個大分頁都不會展開，只有回去按同一顆鈕才展得開。
使用者截圖裡「路段管理」被選中、卻是收合狀態，就是這個狀態。

而那份記錄其實**一點用都沒有**：這支程式重新整理一律回到操作首頁，
要回到某一頁就一定得點它一下。也就是說「記住收合」唯一做得到的事，
就是製造使用者回報的那個毛病。

### 處置

- 收合狀態改成**只留在記憶體**，並在載入時清掉舊的 localStorage 鍵
 （否則老使用者一打開還是收的——那正是他遇到的情況）。
- 新增 `gotoView()`：**任何一條通往某一頁的路徑**都先把那一頁展開再換頁。
  ⚠️ 不是只有側欄那一排——`roadAdminButton`、`renameBtn`、`nextBtn`、
  頁內的 `[data-go]` 都會走到同一頁，逐一改過去。
  使用者截圖裡的「路段管理」正是 `roadAdminButton`，只改側欄那一排會漏掉它。
- **不可以寫進 `go()`**：`go()` 在還原分頁時也會被呼叫。
- 收合鈕照常可以收（停在同一頁時把側欄收乾淨），那才是它存在的意義。

### 三支同步

| 程式 | 分類（整區）收合 | 某一頁的小分頁收合 | 本次改動 |
|---|---|---|---|
| 交通服務水準 | 標題本身就是切換鈕，點了就展開 | 記在 localStorage → **改成只留記憶體＋點大分頁就展開** | ✅ |
| 全日交通量 | 分類標題**同時是分頁**，點它只換頁不展開 → **點它就展開** | （同一層） | ✅ |
| 路口轉向 | 標題本身就是切換鈕，點了就展開 | 記在 localStorage → **改成只留記憶體＋點大分頁就展開** | ✅ |

⚠️ 全日交通量的分類收合**仍然記得住**，因為它的分類標題永遠在畫面上，
重新整理之後看得到也點得開，記住它是有意義的；另外兩支不是這樣。

### 守門與反面測試

| 守門 | 新增／修改的條目 | 反面測試（把修正拿掉） |
|---|---|---|
| `ts2028/e2e-nav-collapse.mjs` | ④ 收合後離開再點回來要展開／④-2 停在原地再點一次也要展開 | ❌ ④ 0/2、❌ ④-2 前置 |
| `n2064/scripts/e2e-nav-collapse.mjs` | ④-2 收合中點分類標題要展開 | ❌ ④-2 0/6 |
| `g2164/scripts/e2e-nav-collapse.mjs` | ④ 離開再點回來要展開／④-2 重新整理後是展開的 | ❌ ④ 0/3 |

三支的反面測試都**先紅後綠**，紀錄在本輪對話中。
另外每一支都補了「收合鈕仍然收得起來／再按一次展開回來」，
避免為了自動展開而把收合做死——只驗展開的話，那樣改也會全綠。

⚠️ 舊版的第④條是「重新整理之後收合狀態還記得」，那是**我自己加的**，
不是使用者要求的；它和使用者這次的要求直接衝突，所以在
交通服務水準與路口轉向改寫成上表的條目（全日交通量的仍然保留，理由見上）。

---

## 二、LOS 圖表頁多一層頁面標題

### 使用者原話

> 「紅框的標題和說明看起來很突兀，拿掉好了，不知為什麼這個分頁多了這個標題和說明，
>   感覺是少了東西，但其他分頁沒有這個問題」

### 實測到的成因

`#charts` 這一頁有**兩層標題連著**：

1. 頁面標題　`VISUAL CENTER／LOS 圖表／各路段 LOS、歷季旅行速率…`
2. 區塊標題　`LEVEL OF SERVICE／各路段 LOS 圖／每路段一張圖…`

②是 2026-09-13 依使用者「各路段LOS圖　標題文字消失了!!」補上的。
補完之後這一頁就變成**全站唯一「標題疊標題」的頁**，
而且①講的三件事，②③④三塊底下又各自再講一次。

清點過全部 15 頁：有頁面標題的共 10 頁，底下都只有一層，只有這一頁是兩層。

### 處置

拿掉①，保留②③④。上方 header 本來就寫著「LOS 圖表」，
不會因此不知道自己在哪一頁。

### 守門

`e2e-chart-page.mjs` 第③組改成**成對**的兩條：

- 圖表頁**不可以**有 `#charts > .title`（＝不可以再長回兩層標題）
- 「各路段 LOS 圖」這個區塊標題**必須還在**

⚠️ 只驗前者的話，把②也一起刪掉照樣全綠——而那正是使用者
2026-09-13 抱怨過的「標題文字消失了」。兩條要一起看才守得住。

---

## 三、路口轉向的分類標題字級

使用者 2026-09-14 只在交通服務水準回報「分類標題文字比大分頁還小」，
但路口轉向量出來一模一樣（標題 13px／分頁 16px），依
「我們踩過的雷，請確保三份程式都不會再踩到」一併修正為 16px／800。

⚠️ 守門的門檻**不可以寫成絕對數字**。我第一次在交通服務水準就是寫
「至少 13px」——那是只想著「比原本的 10.5px 大就好」，結果 13px 仍然
比 16px 的分頁小，使用者只好再回報一次。`e2e-nav-zone.mjs` 兩支現在量的是
**關係**：分類標題不可以比它底下的分頁小，並附一條前置確認真的量到分頁字級
（量到 0 的話那條會恆真）。

順帶清掉 `g2164/app/globals.css` 裡 `.nav-zone-toggle` 重複的 `font-weight`
（700 與 800 疊在同一條規則裡，永遠是後者贏，照抄的人會弄錯）。

---

## 三之二、LOS 圖的 Y 軸刻度和柱高不是同一把尺（我自己看畫面抓到的）

使用者**沒有**回報這一項。是補上 Y 軸之後照約定用肉眼看畫面時抓到的。

### ① 最下面那一格刻度被切掉一半

刻度是 `top:100%` ＋ `translateY(-50%)`，也就是**跨在軸線上**；
而 `.bars` 是 `overflow-y:hidden`（為了讓圖在窄視窗可以左右捲），
超出去的下半個字直接被裁掉。實測：字框 198～211px，容器只有 205px。
LOS 的「F」與旅行速率的「0.0」都中招。

### ② 柱頂對不到自己的等級——**這是圖說謊**

柱高是 `losRank / 6`（A=6/6 最高、F=1/6 最矮），
刻度卻是把六個字母**平均分**在軸上（第 i 個放 i/5）。兩把尺不一樣：

| 等級 | 柱頂（離頂） | 刻度（離頂） | 差 |
|---|---|---|---|
| A | 0% | 0% | 0 |
| D | 50% | 60% | **10%** |
| F | 83.3% | 100% | **16.7%** |

實測 D 的柱頂與 D 刻度差 17.5px。照著軸看，D 會被讀成 B 跟 C 之間。
補 Y 軸以前沒有刻度可以對照，所以這個錯誤是跟著 Y 軸一起進來的。

### 處置

格線、刻度、軸線、柱子四樣東西本來各用各的基準，現在一律對齊**同一個繪圖區**
（上緣 20px、下緣 9px、左 76px、右 10px）：

- 下緣留 9px（字高 13px，半個字 6.5px），最後一格刻度整個在容器裡。
- 格線改由 `::before` 畫在繪圖區上，百分比自然就是刻度的位置
 （原本畫在整個容器上，每一條都偏在刻度中間，實測 B 差 15px）。
- 刻度帶自己的 `ratio`（＝該等級的柱高比例），畫面與匯出的 SVG
  **都**用 `1 - ratio` 定位——不是兩邊各算一次。

### 守門與反面測試

`e2e-chart-page.mjs` 第⑤組：

- ⑤ 每一根柱子的頂端都落在自己那一級的刻度上（**量畫面像素**，不讀原始碼推算）
- ⑤-2 匯出圖檔（同一份 chartModel 畫出來的 SVG）也是
- 兩條各自附一條前置（量到 0 根就會紅，不會恆真）

反面測試（把刻度改回平均分）：**❌ D 差 17.5px**，且 ⑤-2 的前置一起紅
（刻度標籤變成物件字串，對不到等級）——沒有讓它安靜地通過。

---

## 四、本輪跑過的守門

交通服務水準：`e2e-nav-collapse`、`e2e-chart-page`、`e2e-view-isolation`、
`e2e-nav-sections`、`e2e-nav-coverage`、`e2e-chart-layout`、
`e2e-figure-note-layout`、`e2e-nav-zone`、`e2e-nav-scroll-position`、
`e2e-text-wrap` ——全綠。

全日交通量：`e2e-nav-collapse` 全綠（含反面測試）。
路口轉向：`e2e-nav-collapse`、`e2e-nav-zone` 全綠（含反面測試）。

⚠️ 本輪依 2026-09-14 談好的流程，屬於「頻繁修改期間只跑必要檢查」，
**不是**完整大檢查。完整大檢查排在主工具列升級完成之後。

---

## 主工具列上線（三態：鏡子／脫離／回歸）

### 使用者原話

> 「主工具列＝彙整所有圖表工具列的篩選條件」
> 「圖可以自己改，但只影響那一張」
> 「針對共同的篩選條件……圖表自身的工具列仍舊要與主工具列同步」
> 「主工具列也多一個全部回歸鈕……避免有部分圖表忘記點選回歸」
> 「有圖表不適用的條件，該圖表就單獨顯示不適用」
> 「季度一律只做起訖區間，預設起＝迄」

### 這一支的條件

季度起訖區間（預設起＝迄＝最新一季）、路段多選、日別（平日／假日／並列）、
方向（方向1／方向2／並列）、尖峰（代表尖峰／上午／下午／並列）。

### 不可以弄錯的兩件事（都已寫成測試）

1. **先篩再挑最差。** 彙總的每一列是「同一組（計畫・年・季・路段・日別）
   四筆裡最差的那一筆」。主工具列篩了方向或尖峰時，必須**先把候選縮到符合條件
   的那幾筆、再挑最差**；拿已經建好的 `state.summaries` 去篩，只會剩下
   「剛好代表值就是方向1」的那幾列，其餘整列消失——使用者會以為那些路段
   那一季沒有資料。`main-filters.test.mjs` 用**手算**的期望值釘住
   （方向2 下午 LOS C、ratio 0.52）。
2. **預設條件下一個數字都不可以變。** 同一份明細、同一支排序函式、同一種分組鍵，
   `summariesFor()` 必須與升級前 `rebuild()` 的結果逐筆相同。

### 「並列」是呈現方式，不是篩選

平日＋假日、雙向、上午＋下午三個「並列」在資料層**一筆都不篩掉**；
把它當篩選的話，「全部」與「並列」就沒有差別了（已有測試）。

### 起＝迄的意思

起＝迄是「還沒動過區間」的預設狀態：一次只看一季的區塊取結束季度，
**歷季類的區塊（LOS 圖、歷季旅行速率、趨勢、三段分法）看全部季度**。
拉開之後才真的篩。這一條三支程式相同。

### 成果交付與結論草稿維持獨立

使用者明確裁示這兩頁不自動跟著主工具列跑（報告常常要輸出一份和畫面不同的範圍），
改為各加一顆「套用主工具列目前的條件」。`e2e-apply-main.mjs` **兩件一起驗**：
不驗「維持獨立」的話，一個「其實一直自動跟著跑」的實作也會全綠。

### 這一輪順帶抓到的兩件事

- **測資不像真資料**：`e2e-column-filter.mjs`／`e2e-reveal.mjs` 造的假明細只給
  `period`、沒有 `year`／`quarter`，而彙總的分組鍵用得到那兩欄，於是兩季被併成
  一組、彙總從 6 列縮成 3 列。已補上，並在原地寫明為什麼不可以省。
- **舊守門打在已經沒有人讀的欄位上**：`e2e-trend.mjs` 用 `trendState.day` 切日別，
  但日別已經升到主工具列。改成走這一塊自己的 `#trendDay` 下拉（＝使用者的操作）。

### 守門

`main-filters.js` ＋ `main-toolbar.js`（純資料、可單獨測）、`main-filters.test.mjs`（9 條）、
`e2e-main-toolbar.mjs`（三態，連數字一起量）、
`e2e-filter-coverage.mjs`（每個條件 × 每一頁：不是真的算了就是寫明不適用，
並反面守「回到預設之後不可以還留著任何一句不適用」）、`e2e-apply-main.mjs`。

---

## 五、旅行速率／行駛速率的標籤掃描（X-65）

### 使用者原話（2026-09-17）

> 「旅行速率和行駛速率是自動掃描文字，選擇對應欄位的數值判讀嗎?
>   例如以前的資料 A1 欄位是旅行速率、A2 是它的數值，B1 是行駛速率、B2 是它的數值，
>   如果哪天調查員把這兩項測值反過來放……程式可以判讀出來嗎?
>   另一種情況，調查員誤植文字 變成有 2 筆旅行速率或 2 筆行駛速率
>   系統可以判別出來，並提出異常警告嗎?」

### 查證結果

**一、是認標籤，不是認欄位位置。** `findLabels()` 逐格掃整張工作表找標籤文字，
`metricAt()` 才從**那個標籤自己**的位置取值（同格冒號後 → 往右最多 5 欄 → 往下），
往右時**碰到第一個非空文字格就停**（所以旅行速率那格是空的時候，
不會一路滑過去撿到行駛速率的數字）。因此把 A／B 兩欄整組對調，讀到的數值照樣
跟著各自的標籤走——已用「正常版面」與「對調版面」兩份矩陣比對，兩者結果完全相同。

**二、重複標籤的守門，原本只有一半。**

| 版面 | 兩筆「旅行速率」 | 兩筆「行駛速率」 |
|---|---|---|
| 有「旅次編號」分隔線（實際報告的主要版型） | ✅ 早就會報錯 | ✅ 早就會報錯（`single()`） |
| 沒有分隔線、退回舊解法 | ✅ 早就會報錯 | ❌ **靜靜挑最近的那一個** |

右下角那一格就是這次補的。舊解法對行駛速率用的是 `nearestMetric()`——
「離這個方向最近的那一個」——找到就用，不數有幾個。而且**物理檢核攔不到**：
兩個行駛速率只要都大於旅行速率、也都不等於它，`implausibleReason()` 一句話都不會說，
`issue` 是空的、`ok=true`，數值就這樣寫進資料庫。

修法：舊解法也照方向的區塊界線數「平均總行駛速率」，超過一個就整張表報錯——
和有分隔線那條路的 `single()` 同一條標準：

> 「上午尖峰」工作表中方向1有 2 個「平均總行駛速率」，系統不會自行挑選或平均，請確認報告只保留一個代表值

**三、誤植成「2 筆行駛速率、0 筆旅行速率」也接得住**：那一趟讀不到旅行速率，
會落到「切出 N 趟旅次，但只有 M 趟讀得到『平均總旅行速率』」這一句。

**四、系統一律不自行對調。** 物理檢核（旅行速率不得大於行駛速率；有延滯時兩者不得相等）
只負責**指出**欄位可能錯位，畫面上明寫「⚠️ 系統不會自行把兩個值對調——那等於替調查資料做決定」。

### 守門

`speed-label-scan.test.mjs`（4 條）：對調後結果不變、兩筆旅行速率要報錯、
兩筆行駛速率要報錯、有旅次編號的版型同樣要報錯。

**反證（實跑）**：把新加的計數整段拿掉，只有「兩筆行駛速率」那一條變紅
（`expected 0 to equal 2`），其餘三條仍綠——確定不是恆真。
第三條的假資料刻意讓兩個行駛速率**都符合物理常識**，否則紅的會是物理檢核、
不是這條守門。

`node parse-harness.mjs test-fixtures` 對 6 份真實測試檔重跑，全部「讀取完整」，
數值與修改前逐筆相同。
