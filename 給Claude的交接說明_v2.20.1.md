# 交通服務水準分析系統 v2.20.1 交接說明

## 不得變更的既有決策

1. 不改變服務水準門檻、代表紀錄選取規則與總延滯計算規則。
2. 代表紀錄的旅行速率、行駛速率與總延滯必須來自同一筆尖峰／方向資料，禁止跨方向混用。
3. 不在來源包新增 `.openai/hosting.json`；既有 GPT Site 使用站台端設定維持原網址。
4. GitHub Pages 正式網址為 `https://baotoast.github.io/Travel-speed/`。

## v2.20.1 相對 v2.20 的變更

- SheetJS 由 0.18.5 更新至官方 0.20.3，瀏覽器與測試使用相同版本。
- 新增 `generate-test-fixtures.mjs`，測試時建立匿名 `.xlsx`／`.xls` 範例，不依賴包外的私人樣本。
- E2E 測試不再引用外部 `../speed-samples`。
- 新增依賴清單一致性檢查。
- 手冊版本及驗證說明更新至 v2.20.1。

## 重現驗證

```text
npm ci
npm run verify
```

預期結果：25/25 單元測試通過、瀏覽器操作測試全數通過、套件安全檢查 0 個已知弱點。
