# 交通服務水準分析系統 v2.20.19 驗證報告

日期：2026-08-31
正式基底：v2.20.16（commit `3b42f8841721ccd8dc8906194c8cf9cd5a0a559f`）
候選來源：Claude 提供的 v2.20.18，經 GPT 複查修正後升版。

## 結論

**沒有變更任何速率、延滯、LOS 門檻或代表紀錄的計算。** 本版修改資料識別欄位
的來源（路段名稱、平假日）、匯入異常訊息與三個複查發現的邊界問題。

## Claude v2.20.18 的主要修正

- 路段名稱優先讀工作表表頭的「站名：」，平假日優先讀調查日期括號；讀不到才退回檔名。
- 站名截到下一個表頭標籤前，並剝除案號，避免同一路段被拆成兩個名稱。
- 排除製表、列印等非調查日期。
- 表頭與檔名不一致時在預覽明確標示。
- 修正匯入錯誤訊息被 `Error.name` 吞掉、來源追溯失敗被誤標為舊資料等問題。
- 新增真 XLSX 的檔名／表頭行為測試，以及 LOS 門檻與代表紀錄黃金值測試。

## GPT 複查新增修正

1. **日別尾端規則補強**：檔名只有路段名稱中的「平日／假日」字樣，尾端沒有日別標記時，
   不再誤判；`-平日-114.0210`、`-(平日)` 與瀏覽器附加的 `(1)` 仍可辨識。
2. **來源衝突二次確認接線**：表頭與檔名不一致時，除了預覽警告，按寫入時會真正要求確認；
   取消即不寫入。
3. **Manager 無效 JSON 回報**：可解析但不是 Project 專案包的 JSON，以及含無效計畫的組合包，
   都列為失敗檔案，不再靜默略過。

## 驗證結果

| 檢查 | 結果 |
| --- | --- |
| `npm test` | 92／92 通過 |
| `node check-version.mjs` | 18／18 通過 |
| `npm run e2e` | 202／202 通過 |
| 舊版 `.xls` 匿名測資 | 完整辨識 4 筆尖峰方向 |
| Excel 匯出結構 | 旅行速率與 LOS 活頁簿通過 OOXML 檢查 |
| 多寬度版面 | 640～1920 px 全分頁無橫向溢出 |

本次未取得使用者實際調查原始檔，因此不宣稱完成真實檔案逐筆核對；測試使用交付包內的
匿名 XLS／XLSX 測資與瀏覽器端到端流程。

## 未變更範圍

- 原始 Excel 尖峰四筆辨識與數值欄位讀取規則
- 代表紀錄選取方式
- 旅行速率、行駛速率、路段延滯、交叉口延滯與總延滯計算
- 公告速限、速限比與 LOS 門檻
- 重複資料鍵值與季度歸屬
- Project／Manager 專案包既有資料欄位
