# 交通服務水準 v2.20.68 驗證報告

## v2.20.68（2026-09-21）GPT 獨立風險導向複查

- 正式基準：`BaoToast/Travel-speed` `main`，複查前本機 HEAD、`origin/main` 與 GitHub 遠端 `main` 均為 `3b95601bc3497d9c01609fd0ab0d3e1483b0c1d2`，工作樹乾淨。
- 候選來源：Claude 提供的 v2.20.67 完整專案與給 GPT 的說明；候選 ZIP SHA-256 為 `d73456617ba94333fbd419a5980512b8ef7dab1c00b074db80a0aa866ac8a41c`，與說明記錄相符。
- 風險分級：**高風險／全專案複查**。本次涵蓋 Parser 日期掃描、state／備份、異常確認指紋、計畫主鍵改名、圖表說明與跨模組 UI／E2E，不能採 Claude 所稱「中低風險」。
- Claude 宣稱 `npm test` 全綠，但 GPT 在原候選上實跑為 **278 項：275 通過、1 失敗、2 略過**；失敗原因是新確認訊息使用守門禁止的位置代稱「上面的」。已改為直接指名「要編輯的計畫」選單。
- GPT 另發現三段組成說明的最差 LOS 並非取實際資料，而是「有壅塞就猜 F、只有尚可就取壅塞起始級」。這會把實際最差 D 說成 E，或把實際最差 E 說成 F。已由 `buildBandSeries()` 保存每季實際 `worstLos`，畫面只讀該值，並新增永久回歸測試。
- 依工程規則，GPT 有額外程式修正時版號必須推進，因此 Claude 候選 v2.20.67 改為正式 v2.20.68。
- Node／契約／回歸：**279 項，277 通過、0 失敗、2 略過**。兩項略過需要正式 Repository 未提供的真實平／假日配對檔與同檔雙日期情境，不得列為通過。
- Playwright E2E：先建立 6 份匿名 fixtures，依 `package.json` 順序逐支執行，**全部 56 支通過**；額度中斷後從第一個未完成腳本續跑，沒有重跑已確認部分，也沒有並行。
- `e2e-filter-coverage.mjs` 反證：在隔離副本故意移除 roads 的 `applied` 標記後，以退出碼 1 精確抓到「各路段 LOS 圖」「各路段歷季旅行速率」寫不適用卻變動；還原後聚焦測試全綠。
- `check-version.mjs`、`glyph-guard.mjs` 全綠；本專案沒有 TypeScript、lint 或 production build script，均為不適用，未虛報通過。
- 本輪沒有使用者真實報告檔，因此未執行 `verify-against-summary.mjs <檔案>`；匿名 fixtures 不冒充真實業務檔驗證。
- 功能正式版基準 commit：`4700d23d50ed71774f5aff021234a2337481a1ce`（`Release v2.20.68 after risk-based review`）。
- GitHub Actions：建置與測試 run [`35545434854`](https://github.com/BaoToast/Travel-speed/actions/runs/35545434854) 成功；Pages run [`35545434337`](https://github.com/BaoToast/Travel-speed/actions/runs/35545434337) 成功，兩者均對應 `4700d23...`。
- GitHub Pages：`https://baotoast.github.io/Travel-speed/` 首頁顯示 v2.20.68；線上 `index.html`、`app.js`、`trend.js`、`period-date.js`、`direction-pair.js`、`chart-levels.js` 與 v2.20.68 PDF 手冊 SHA-256 均逐檔等於 Repository。新版手冊 HTTP 200，舊 v2.20.66／v2.20.67 手冊 URL 均為 404。
- 關鍵線上雜湊：`index.html` `594f286c82a3897fee762869a4fa88be5978b5503040d7e5395da8a5c11614b1`；`app.js` `9067257861b563e67360e6d0387ed24c2666ea7a5f6cd758bdfce7f3fa94a4fc`；`trend.js` `c788215537833c1b5bb149481986fd7c7de542d79b8f2cc8c6dd25f4f8c621b4`；手冊 `0208932a9acb90ed772559a86104043ab0d42ccf283a17380be7b1c7ac284462`。

## v2.20.67（2026-09-20）使用者回報 2 項 ＋ Claude 大檢查 2 項

### 使用者回報（一）確認過的「方向對應不一致」每季匯入又冒出來

> 「我將路段車流方向手動修正為 北上/南下，導致我每季匯入資料，
>   每一次都抓出車流方向不一致的問題。
>   怎會把我手動修改這間事當作異常來回報給我呢?」

**根因不是他改了方向名稱。** `issueFingerprint()` 把**整段說明文字**算進
指紋裡，而「方向對應不一致」的說明是

> 不同報告把這個方向寫成：**A--->B、B--->A**。請確認各季報告的旅次順序…

粗體那一段是**目前所有季度出現過的寫法全部列出來**。於是：

- 又匯入一季、那一季多了一種寫法 → 說明變 → 指紋變 → 他按過的確認對不上
- 甚至同樣內容、只是匯入檔案順序不同 → `Set` 的排列順序不同 → 指紋一樣會變

實跑重現：

```
多一種寫法   → 指紋相同? false
只是順序不同 → 指紋相同? false
```

附帶後果：舊指紋永遠不會再對上，`state.ackedIssues` **只進不出**，
使用者看不見地一直長大（正是他在第二個問題裡擔心的「資料會累積越來越多」，
只是發生在他看不到的地方）。

#### ⚠️ 不可以把 `detail` 從指紋裡整個拿掉

原本的註解寫得很清楚，而且**是對的**：「異常變化」的說明帶著數字
（總延滯增加 30.7%），數字一變就該重新提醒，否則使用者會在不知情的情況下，
把後來更嚴重的變化一起按掉。

所以正確解是**逐類別決定**：

- 每一個 `issues.push` 補上固定代碼 `code`（9 個，不可重複）
- 指紋用 `code` 取代 `type` —— 同一個 `type`（「數值異常」）底下有兩種檢查，
  它們的 type／期間／路段／項目完全一樣，**以前只靠 `detail` 區分**
- 「方向對應不一致」標 `ackScope: "identity"`，指紋不看說明文字
- 其餘類別維持原本行為
- 檢查跑完後清掉「再也對不上任何一筆現存異常」的孤兒紀錄（`pruneOrphanAcks`），
  而且**只清孤兒**

#### 守門與兩個方向的反證

`issue-ack-stability.test.mjs`（8 條，含**對照組**與前置檢查）。

**反證 A** —— 拿 v2.20.66 交付包裡**原封不動**的 `app.js` 跑：

```
ok  1 - 前置：取得 app.js 裡的 issueFingerprint，而且它真的算得出東西
not ok 2 - 一、方向對應不一致：多一種寫法，確認不可以失效
not ok 3 - 一、方向對應不一致：同樣的寫法只是順序不同，確認也不可以失效
ok  4 - 二、對照組：異常變化的數字變了，指紋就必須變（刻意保留的行為）
not ok 6 - 四、每一個 issues.push 都帶著固定代碼
not ok 7 - 五、孤兒確認紀錄清得掉，而且只清孤兒
```

**反證 B** —— 改成「把 `detail` 整個拿掉」這種**錯誤修法**：

```
not ok 4 - 二、對照組：異常變化的數字變了，指紋就必須變
```

兩個方向都擋得住：真的 bug 會紅，錯誤的修法也會紅。

#### 使用者另外提的一半，本版刻意不動

> 「如果是出現我打 北上/西行 這種不是雙向的車流方向才算異常吧」

那是**另一個檢查**（檢查使用者自己取的顯示名稱是否成對），
與現有檢查要防的「調查員換了旅次起跑方向、跨季比較拿相反方向互比」
是兩件不同的事。兩者可以並存。**已回報並等待使用者裁示，本版不動。**

### 使用者回報（二）重點路段總覽看起來像待辦清單

> 「想問重點路段總覽用意是什麼呢?他告訴了我哪些路段有異常變化嗎?
>   那我看完後我要怎麼點選確認，讓之後不會一直出現?不然資料會累積越來越多」

查證結果：它是**排序不是清單**。`priorityRows()` 對每條路段×日別拿
**最新一季與前一季**比，符合條件才上榜並給分；每次都重新計算，
下一季不再惡化就自己掉出榜單，所以**不需要也沒有「已確認」**。

但它長得和上面那張「檢查結果」一模一樣（同樣是表格、同樣在資料維護頁），
而畫面上**一個字都沒說**它不用確認 —— 使用者因此以為漏了一顆鈕、
而且會擔心資料累積。**會誤導使用者的就要修。**

抬頭補上：這是排序不是待辦清單、只比最新一季與前一季、不需要也沒有
「已確認」、不會累積；另補一行「上榜條件」與建議欄兩種寫法的意思。

守門為上一支的第 6 條。⚠️ 它刻意**不**驗「有沒有確認鈕」：
驗那個等於把「現在沒有鈕」釘死，哪天真要加鈕反而被自己的測試擋住。
驗的是**畫面有沒有把話講清楚**。

## v2.20.67（2026-09-20）Claude 大檢查：另外兩項修正

### 一、手冊說「計畫設定」，畫面上那一頁叫「建立與管理計畫」

使用者 2026-09-13 指定三支統一改名為「建立與管理計畫」，`titles.setup`
與側欄都改了，但**兩處說明文字沒跟著改**：

- `app.js` 內嵌手冊第一章：「進入『計畫設定』，輸入公司計畫編號與完整名稱。」
- `index.html` 新手操作順序第 1 步標題：「計畫設定」

使用者照說明去側欄找「計畫設定」，找不到。這正是大檢查規則 4.5
「手冊與系統要逐項交叉比對」要抓的那一類，而且**沒有任何測試在看**。

判定：**系統是對的，手冊錯了** → 改說明文字。另有一處 `//` 註解也一併更新。

新守門 `manual-names.test.mjs`：把每一次改名的舊名字列成一張表，
掃 `app.js` 與 `index.html` 的說明文字，舊名字一個都不可以剩。
區塊註解與單行註解裡的歷史敘述放行（那不是使用者看得到的文字）。

**反證（實跑，用 v2.20.66 交付包裡原封不動的 app.js／index.html）**：

```
ok 1 - 前置：titles 讀得到，而且含有本次要守的那幾個名字
not ok 2 - 說明文字裡不可以再用改名前的分頁名稱
    app.js 的說明文字還寫著「計畫設定」，但畫面上那一頁叫「建立與管理計畫」
```

### 二、長計畫名稱／長編號沒有上限，也沒有換行保護（三支共通，只有這一支漏了）

使用者 2026-09-10 實測（附截圖）：「長計畫名稱會撐破卡片」「忘記限制計畫編號
（三個程式都是），當編號過長時會遮蓋到計畫名稱」。

另外兩支早就做了——路口轉向 `lib/final-features.ts` 的
`PROJECT_NAME_LIMIT = 40`／`PROJECT_CODE_LIMIT = 20` ＋ `app/globals.css`；
全日交通量 `tests/project-name-limit.test.mjs`。**這一支兩件都沒有**，
而且沒有任何檢查在看，所以漏了十天沒人發現。

本版補上，**兩件一起做**（缺一不可）：

- **上限**擋新輸入：`maxlength`（擋鍵盤）＋ `capText`（擋貼上），
  送出時再過一次。數字與另外兩支一致（40／20）。
  `capText` 取「上限與原值長度的較大者」，所以**既有的超長名稱不會被
  無聲截短**——直接 `slice` 會在使用者只是點一下欄位時掉字。
- **換行**擋舊資料：`header > div:first-child { min-width: 0 }` ＋
  `overflow-wrap: anywhere`。flex item 的預設 `min-width: auto` 正是
  截圖裡「編號蓋到名稱」的直接原因。

新守門 `project-name-limit.test.mjs`（5 條，含前置檢查）。

**反證（實跑，用 v2.20.66 交付包裡原封不動的 app.js／styles.css）**：

```
ok 1 - 前置：原始檔讀得到內容（不然下面全部恆真）
not ok 2 - (甲) 兩個上限都宣告了，而且數字與另外兩支一致
not ok 3 - (甲) maxlength 與 capText 兩條路都接上了
not ok 4 - (甲) capText 不可以把既有的超長值一刀砍掉
not ok 5 - (乙) 版面這一側也擋住了：既有超長值要換行而不是撐開
```

前置那一條維持綠，證明 4 項紅不是「什麼都沒量到」。

### 三、使用者 2026-09-20 授權的項目（本支承接的部分）

#### 1. 「X 級以下路段占比」可選：**使用者取消，已逐位元復原**

使用者原話：「他是指判斷壅塞的，所以的確不需要 X級以下路段占比功能，
**你能復原原本的樣子嗎?這一個可以不用做了，請不要影響到原有的功能**」

拿**原始的 v2.20.66 壓縮包**解開逐位元比對，證明沒有殘留：

| 比對對象 | 結果 |
|---|---|
| 趨勢圖整個區段（`$("trendRoad").innerHTML` → `$("trendRoad").onchange`，100 行） | md5 `e699e2992961082834e7479d3390b1d3`，**與原始版完全相同** |
| `index.html` 的 `.trend-controls` | 逐位元相同 |
| `bandsFor`／`bandLabels`／`renderBandRule`／`renderRuleShortcut` | 四個函式的 md5 與原始版完全相同 |

全檔剩下的差異逐項確認，沒有一項與「X 級以下」有關。
（`bandLabels().congestedShareLabel` 這個**動態指標名稱本來就存在**，
不是這次加的，也沒有被拿掉。）

#### 2. 方向名稱成對檢查（三支同步）

- 判定走 `direction-pair.js`（三支逐位元同一份規則），契約表三支逐位元相同。
- 「北上／西行」會報；「方向1／方向2」「往台北／往高雄」「北屯路／文心路」不報。
- ⚠️ 走 `ackScope: "identity"`，而且 fromPeriod／period 留空——
  **按過的確認不會因為又匯入一季而失效**（這一支正是那個 bug 的原產地）。

#### 3. 調查日期（三支同步）

- `surveyDateCellsOf(wb, deep)`：候選一律**全表掃描**；
  系統**實際採用**的那一個仍以表頭優先（表頭找不到才往下掃整張表）。
- 候選清單的**第一個一定是系統實際採用的那一個**。
- 異常檢查新增「調查日期不只一個」，那一列多一顆「指定調查日期」下拉；
  挑完＝同時寫覆寫（`state.surveyDateOverrides`）並記為已確認。
- 覆寫**必須是候選之一**才採用，而且跟著**兩種備份**走
  （`backup-completeness.test.mjs` 的 `MUST_TRAVEL` 已加入）。
- 尖峰明細與尖峰彙總的「期間」欄底下多一行「調查日 …」，
  並各有一顆「顯示調查日期」開關（**寫同一個設定**，預設開）；
  讀不到時寫「原始檔讀不到日期」，**不留白**。
- ⚠️ `period-date.js` 補上 `surveyDateInYearStyle()`（原本只有另外兩支有），
  否則明細上的日期寫不出民國年。

#### 4. 圖說第 3、4 級（三支同步）

- `chart-levels.js` 是同一份程式的 ES5 版；因為模組語法不同，**不能用 SHA-256 釘內容**，
  改用「同一組輸入、輸出逐字相同」釘住（`chart-levels.test.mjs` 第三項）。
- 接上四張圖：各路段 LOS 圖、各路段歷季旅行速率、歷季趨勢、三段分法。
- ⚠️ LOS 圖那一張的壅塞起始等級走 `bandHitFor()`（會解析「季別區間 × 路段」的覆寫），
  **不是** `bandsFor()`——用錯的話文字會說「還沒進壅塞段」而圖上明明已經紅了。

#### 本次新增守門與反證（全部實跑）

| 守門 | 反證方式 | 結果 |
|---|---|---|
| `survey-date-pick.test.mjs`（17 項） | 指紋無條件多加一格 | 16 pass / 1 fail |
| 同上 | 候選只掃表頭 | 16 pass / 1 fail |
| 同上 | 覆寫不檢查是否在候選內 | 16 pass / 1 fail |
| 同上 | 明細不顯示日期 | 16 pass / 1 fail |
| 同上 | 專案包不收覆寫 | 16 pass / 1 fail |
| `chart-levels.test.mjs`（3 項） | — | 契約 30 列全過 ＋ 輸出逐字比對 |
| `e2e-chart-levels.mjs`（26 項，新增進 e2e 鏈） | 把 levels 接線拿掉 | **5 項轉紅** |

還原之後全部回到全綠。

⚠️ 另外，既有的 `wording.test.mjs` 在這一輪抓到我自己寫的兩句用了位置代稱
（「請在這一列右邊的下拉選單…」），已改成指名控制項（「指定調查日期」選單）。
這正是那一支守門存在的理由。

### 本版沒有變更任何計算

LOS 門檻、三段分法、代表紀錄挑選、旅行／行駛速率與延滯的算法一行都沒動。
`los-and-selection-golden.test.mjs` 的黃金值不變。

### 證據界線

- 這個容器連不到 `baotoast.github.io`，**無法逐位元比對線上原始檔**。
- 沒有 Microsoft Excel／Foxit，無法複驗 GPT 的 Office 開啟檢查。


## GPT 獨立風險導向複查（2026-09-19）

- 正式基準：`BaoToast/Travel-speed` `main`，複查前 HEAD／`origin/main`／GitHub 遠端 `main` 均為 `8420e1d7c2d481a98cfdf50f1b87d71e927a4782`，工作樹乾淨。
- Claude 第二次複查指出：三句 `applied: true` 的說明仍帶 `data-inapplicable`，造成「文字說已套用、機器屬性說不適用」的語意矛盾。GPT 檢查 `inapplicableHtml()` 與全部三個 applied call site，確認屬實。
- 風險分級：**低風險／共用說明 helper 與對應 E2E 守門**。本次不改 parser、資料、公式、篩選結果、畫面文字或匯出；但 helper 為共用程式，仍抽查全部呼叫位置並執行完整 Node／Playwright 回歸。
- 修正：`options.applied` 為真時只輸出 `data-note-kind="applied"`，不再輸出 `data-inapplicable`；一般不適用說明維持原行為。
- 新增永久守門：三種已套用說明都必須由 E2E 實際觸發，且 applied／inapplicable 屬性互斥。修正前守門以退出碼 1 精確列出三句矛盾說明；修正後聚焦 E2E 全綠。
- 反證：在獨立暫存副本故意移除三個 `applied` 標記後，`e2e-filter-coverage.mjs` 以退出碼 1 失敗；季度、路段、日別三種條件均抓到「各路段 LOS 圖（losChartSection）」「各路段歷季旅行速率（speedTrendSection）」寫不適用卻實際變動，證明反向守門沒有被架空。
- 依工程交接規則，只要 GPT 有額外程式修正就推進版號，因此由 Claude 原包 v2.20.65 升為正式 v2.20.66。
- Node／契約／回歸：共 230 項，**228 passed、0 failed、2 skipped**；兩項需要正式 Repository 未提供的真實平／假日配對檔與同檔雙日期情境。Claude 在自己的環境宣稱 253／253 全過，但本輪無法取得其真實檔案，故不把該數字列為 GPT 已獨立證實。
- Playwright E2E：先產生 6 份匿名 fixtures，56 支依序執行，**56／56 通過**；沒有並行。
- `check-version.mjs`、`glyph-guard.mjs` 全綠；本專案沒有 TypeScript、lint 或 production build script，不能把不適用項目記為通過。
- 手冊：v2.20.66 PDF 共 25 頁，已逐頁渲染並檢視；版號、日期、頁尾正常，未見裁切、重疊、缺字或異常空白頁。
- 本次沒有改 Excel 匯出程式或 UI 版面；v2.20.65 已完成真實 Excel 開啟與 1536×864／1366×768 逐頁驗證，本輪完整 E2E 另含多寬度版面與 OOXML 檢查，未重複人工 Office 驗證。
- 正式功能 commit：`111892943b2b83539062e5bf10d224690b25bda0`。
- GitHub Actions：建置與測試 run `35446287153` 成功；Pages run `35446286843` 成功，兩者皆對應正式功能 commit。
- GitHub Pages：線上首頁顯示 v2.20.66；`index.html`、`app.js`、v2.20.66 手冊 SHA-256 均逐檔等於 Repository；新版手冊 HTTP 200，舊 v2.20.65 手冊 URL 為 404。

## 前一版 v2.20.65 的 GPT 獨立風險導向複查（2026-09-19）

- 正式基準：`BaoToast/Travel-speed` `main`，複查前 HEAD／`origin/main`／GitHub 遠端 `main` 均為 `58167e0fe63c1de9812660572eabd15b598750f4`，工作樹乾淨。
- 候選來源：Claude 提供的 v2.20.65 完整專案、試用版、手冊、給 GPT 的說明與 SHA-256 清單；五項原始交付雜湊均核對相符。
- 風險分級：**高風險／全專案複查**。雖然使用者已確認 v2.20.54 至 v2.20.65 的版次躍進是多輪試用修正的正常結果，但本版涵蓋 parser、全站主篩選、LOS／速限／三段規則作用域、圖表、匯出、導覽與大量共用資料流，不能只按 Claude 宣稱的檔案範圍抽查。
- GPT 額外發現並修正：
  1. `package-lock.json` 根版本仍為 v2.20.61，與 `package.json` v2.20.65 不一致；已修正並新增鎖檔名稱／版本守門。
  2. `wording.test.mjs` 使用 URL pathname 組 Windows 路徑，造成 `D:\\D:\\...`；改用 `fileURLToPath()`。
  3. PDF 手冊下載表格被拆到單獨一頁；調整列印分頁後，完整表格與後續段落同頁呈現，頁數由 26 頁縮為 25 頁。
  4. 移除三支未被套件流程引用、含特定環境路徑的候選暫存腳本；保留 E2E 使用的 `_toolbar.mjs`。
  5. `e2e` 指令內硬呼叫 `npm run fixtures`，在沒有全域 npm 命令的環境無法開始；改為直接執行同一支 `generate-test-fixtures.mjs`，並讓守門驗證實際目的而非綁死套件工具名稱。
- Node／契約／回歸：共 230 項，**228 passed、0 failed、2 skipped**。兩項 skip 均要求真實成對／雙日期業務檔，未宣稱通過。
- Playwright E2E：先執行 fixtures，56 支依序執行，**56/56 通過**；中斷前 41 支與續跑 15 支均有完成紀錄，沒有並行執行。
- 核心計算核對：預設 LOS A≥0.8、B≥0.6、C≥0.5、D≥0.4、E≥0.2；未知速限為 `?`；代表紀錄依最差 LOS、最低 ratio、最低旅行速率挑選；總延滯任一組成缺值即為 `null`；預設篩選結果與舊資料流一致。
- 版面：以實際單檔試用版、3 季匿名 fixtures，逐頁檢查 18 頁 × 1536×864 與 18 頁 × 1366×768；36 張畫面均無水平溢出、文字重疊、圖表超界或表格裁切，瀏覽器亦無 JavaScript 例外。
- Microsoft Excel：由正式 UI E2E 產生 `e2e-trend-excel.xlsx`，再以本機 Microsoft Excel 唯讀開啟；無損毀／修復錯誤，3 個工作表完整，圖表頁 2 個原生圖表均可由 Excel 匯出且人工檢視正常。
- 手冊：重建為 25 頁 PDF，已逐頁渲染與人工檢視；沒有空白頁、重疊或截斷。
- 真實業務檔：本輪沒有取得新的使用者真實報告檔，因此未重跑 `verify-against-summary.mjs <檔案>`；不得把匿名 fixtures 說成真實業務檔驗證。
- 正式功能 commit：`2888a319032ec7d7bcba59e794ef499157e1bc67`；第一階段交接 HEAD：`18d2e3558e75a48fac01cd13848f12a584b8a6fb`。
- GitHub Actions：建置與測試 run `35410073884` 成功；Pages run `35410073205` 成功，兩者皆對應 `18d2e355...`。
- GitHub Pages：`https://baotoast.github.io/Travel-speed/` 已顯示 v2.20.65。線上 `index.html`、`app.js`、`main-filters.js`、`main-toolbar.js`、`los-rule-scope.js` 與 v2.20.65 手冊 SHA-256 均逐檔等於 Repository；新手冊 HTTP 200，舊 v2.20.54 手冊 URL 為 404。

---

- 驗證日期：2026-09-18
- 驗證基準：大檢查（Fable 5.1）修正版，基於 v2.20.64
- 計算：LOS 門檻＝《2022 年臺灣公路容量手冊》表 16.12（A≥0.8、B≥0.6、C≥0.5、D≥0.4、E≥0.2）逐值核對；
  `verify-against-summary.mjs` 對真實報告延滯統計表 6/6 相符；代表紀錄挑選手算相符。
- 說明句：e2e-filter-coverage 反向斷言全綠（applied 標記後）；plaintext-markup、wording 守門全綠，
  反證：注入 `**x**`／「按下面的」→ 紅；speed-label-scan 在 v2.20.64 上 2 紅（訊息沒有儲存格）。
- check-version.mjs 全部通過；npm test 全綠。

---

## 前一版（v2.20.64）驗證紀錄


- 驗證日期：2026-09-17
- 驗證基準：Claude 工作版 v2.20.60（本輪為使用者實測回饋的修正，尚未經 GPT 複查）
- 風險分級：**側欄行為與版面**，不含任何計算口徑變更

---

## v2.20.64（2026-09-17）本版驗證重點

- **匯入時「重複」要出聲**（X-66）：使用者問三支程式是否都認標籤、
  欄位換位讀不讀得到、重複會不會指出異常。本支的答案是「都做到了」
  （X-65 剛補完最後一個缺口），另外兩支各自補了自己的洞。
- **稽核表 L**：結論草稿裡兩種分母並排——「A 級 n 筆（x%）」用全部筆數、
  「三段分法佔比」用可判定筆數。已統一成可判定筆數，並把口徑寫進句子裡。
  守門不比字串，直接把兩段的百分比各自加起來驗是不是都 100%。
- **稽核表 M**：Excel 匯出的分組鍵撞到時，後者會**無聲蓋掉**前者。
  已改成數值不同就整份擋下匯出並說出是哪幾筆（數值相同不算衝突）。
- 單元測試 257 條全綠。

## v2.20.62（2026-09-17）本版驗證重點

- **四張圖各自一個大分頁**（X-62）：`e2e-nav-sections` 新增一整段反向驗證
  （四頁真的存在、各自畫得出自己那一塊、不列同名小分頁）；
  `e2e-view-isolation` 逐頁確認每一頁只顯示自己（18 頁）。
- **甲案**：`e2e-main-toolbar`／`e2e-filter-coverage` 的還原值與案例已隨之更新，
  並補上「預設狀態下不可以出現條件式的不適用說明」的反面驗證。
- **「已確認，不再提醒」**：`issue-ack.test.mjs`（6 條）守指紋要帶數值、
  只有「人工確認」類可以按、確認後兩個數字都要寫出來。
- **旅行／行駛速率的標籤掃描補一道守門**（X-65，使用者提問時查出來的）：
  舊解法只數「平均總旅行速率」，行駛速率那一邊用「離最近的那一個」靜靜挑，
  同一個方向被誤植成兩筆行駛速率時不會出聲。詳見第五節。
- 全套 e2e（54 支）與單元測試（251 條）全綠。


## 一、點大分頁不會自動展開它的小分頁

### 使用者原話

> 「當我點選 交通服務水準的 大分頁標題(路段管理)時，它並沒有展開而是保持收合，
>   請修正成自動展開下面的各項小分頁，如同另外兩個程式一樣的做法」
> 「全日交通量程式，我點選分類標題(例如二、參數設定)時，並未自動展開下方的
>   大分頁和小分頁，請同步確認三份程式是否都能自動展開」

### 實測到的成因（不是推測）

用全新的瀏覽器設定檔逐頁量過一次：**全新狀態下是展開的**（路段管理 8 項、
路段速限 2 項、尖峰彙總 3 項…），所以不是「一開始就壞」。

真正的成因是**收合狀態被寫進 localStorage**（`los-nav-collapsed-v1`）：
只要按過一次那顆 ▸ 收合鈕，那一頁就**永遠**是收的，
再怎麼點那個大分頁都不會展開，只有回去按同一顆鈕才展得開。
使用者截圖裡「路段管理」被選中、卻是收合狀態，就是這個狀態。

而那份記錄其實**一點用都沒有**：這支程式重新整理一律回到操作首頁，
要回到某一頁就一定得點它一下。也就是說「記住收合」唯一做得到的事，
就是製造使用者回報的那個毛病。

### 處置

- 收合狀態改成**只留在記憶體**，並在載入時清掉舊的 localStorage 鍵
 （否則老使用者一打開還是收的——那正是他遇到的情況）。
- 新增 `gotoView()`：**任何一條通往某一頁的路徑**都先把那一頁展開再換頁。
  ⚠️ 不是只有側欄那一排——`roadAdminButton`、`renameBtn`、`nextBtn`、
  頁內的 `[data-go]` 都會走到同一頁，逐一改過去。
  使用者截圖裡的「路段管理」正是 `roadAdminButton`，只改側欄那一排會漏掉它。
- **不可以寫進 `go()`**：`go()` 在還原分頁時也會被呼叫。
- 收合鈕照常可以收（停在同一頁時把側欄收乾淨），那才是它存在的意義。

### 三支同步

| 程式 | 分類（整區）收合 | 某一頁的小分頁收合 | 本次改動 |
|---|---|---|---|
| 交通服務水準 | 標題本身就是切換鈕，點了就展開 | 記在 localStorage → **改成只留記憶體＋點大分頁就展開** | ✅ |
| 全日交通量 | 分類標題**同時是分頁**，點它只換頁不展開 → **點它就展開** | （同一層） | ✅ |
| 路口轉向 | 標題本身就是切換鈕，點了就展開 | 記在 localStorage → **改成只留記憶體＋點大分頁就展開** | ✅ |

⚠️ 全日交通量的分類收合**仍然記得住**，因為它的分類標題永遠在畫面上，
重新整理之後看得到也點得開，記住它是有意義的；另外兩支不是這樣。

### 守門與反面測試

| 守門 | 新增／修改的條目 | 反面測試（把修正拿掉） |
|---|---|---|
| `ts2028/e2e-nav-collapse.mjs` | ④ 收合後離開再點回來要展開／④-2 停在原地再點一次也要展開 | ❌ ④ 0/2、❌ ④-2 前置 |
| `n2064/scripts/e2e-nav-collapse.mjs` | ④-2 收合中點分類標題要展開 | ❌ ④-2 0/6 |
| `g2164/scripts/e2e-nav-collapse.mjs` | ④ 離開再點回來要展開／④-2 重新整理後是展開的 | ❌ ④ 0/3 |

三支的反面測試都**先紅後綠**，紀錄在本輪對話中。
另外每一支都補了「收合鈕仍然收得起來／再按一次展開回來」，
避免為了自動展開而把收合做死——只驗展開的話，那樣改也會全綠。

⚠️ 舊版的第④條是「重新整理之後收合狀態還記得」，那是**我自己加的**，
不是使用者要求的；它和使用者這次的要求直接衝突，所以在
交通服務水準與路口轉向改寫成上表的條目（全日交通量的仍然保留，理由見上）。

---

## 二、LOS 圖表頁多一層頁面標題

### 使用者原話

> 「紅框的標題和說明看起來很突兀，拿掉好了，不知為什麼這個分頁多了這個標題和說明，
>   感覺是少了東西，但其他分頁沒有這個問題」

### 實測到的成因

`#charts` 這一頁有**兩層標題連著**：

1. 頁面標題　`VISUAL CENTER／LOS 圖表／各路段 LOS、歷季旅行速率…`
2. 區塊標題　`LEVEL OF SERVICE／各路段 LOS 圖／每路段一張圖…`

②是 2026-09-13 依使用者「各路段LOS圖　標題文字消失了!!」補上的。
補完之後這一頁就變成**全站唯一「標題疊標題」的頁**，
而且①講的三件事，②③④三塊底下又各自再講一次。

清點過全部 15 頁：有頁面標題的共 10 頁，底下都只有一層，只有這一頁是兩層。

### 處置

拿掉①，保留②③④。上方 header 本來就寫著「LOS 圖表」，
不會因此不知道自己在哪一頁。

### 守門

`e2e-chart-page.mjs` 第③組改成**成對**的兩條：

- 圖表頁**不可以**有 `#charts > .title`（＝不可以再長回兩層標題）
- 「各路段 LOS 圖」這個區塊標題**必須還在**

⚠️ 只驗前者的話，把②也一起刪掉照樣全綠——而那正是使用者
2026-09-13 抱怨過的「標題文字消失了」。兩條要一起看才守得住。

---

## 三、路口轉向的分類標題字級

使用者 2026-09-14 只在交通服務水準回報「分類標題文字比大分頁還小」，
但路口轉向量出來一模一樣（標題 13px／分頁 16px），依
「我們踩過的雷，請確保三份程式都不會再踩到」一併修正為 16px／800。

⚠️ 守門的門檻**不可以寫成絕對數字**。我第一次在交通服務水準就是寫
「至少 13px」——那是只想著「比原本的 10.5px 大就好」，結果 13px 仍然
比 16px 的分頁小，使用者只好再回報一次。`e2e-nav-zone.mjs` 兩支現在量的是
**關係**：分類標題不可以比它底下的分頁小，並附一條前置確認真的量到分頁字級
（量到 0 的話那條會恆真）。

順帶清掉 `g2164/app/globals.css` 裡 `.nav-zone-toggle` 重複的 `font-weight`
（700 與 800 疊在同一條規則裡，永遠是後者贏，照抄的人會弄錯）。

---

## 三之二、LOS 圖的 Y 軸刻度和柱高不是同一把尺（我自己看畫面抓到的）

使用者**沒有**回報這一項。是補上 Y 軸之後照約定用肉眼看畫面時抓到的。

### ① 最下面那一格刻度被切掉一半

刻度是 `top:100%` ＋ `translateY(-50%)`，也就是**跨在軸線上**；
而 `.bars` 是 `overflow-y:hidden`（為了讓圖在窄視窗可以左右捲），
超出去的下半個字直接被裁掉。實測：字框 198～211px，容器只有 205px。
LOS 的「F」與旅行速率的「0.0」都中招。

### ② 柱頂對不到自己的等級——**這是圖說謊**

柱高是 `losRank / 6`（A=6/6 最高、F=1/6 最矮），
刻度卻是把六個字母**平均分**在軸上（第 i 個放 i/5）。兩把尺不一樣：

| 等級 | 柱頂（離頂） | 刻度（離頂） | 差 |
|---|---|---|---|
| A | 0% | 0% | 0 |
| D | 50% | 60% | **10%** |
| F | 83.3% | 100% | **16.7%** |

實測 D 的柱頂與 D 刻度差 17.5px。照著軸看，D 會被讀成 B 跟 C 之間。
補 Y 軸以前沒有刻度可以對照，所以這個錯誤是跟著 Y 軸一起進來的。

### 處置

格線、刻度、軸線、柱子四樣東西本來各用各的基準，現在一律對齊**同一個繪圖區**
（上緣 20px、下緣 9px、左 76px、右 10px）：

- 下緣留 9px（字高 13px，半個字 6.5px），最後一格刻度整個在容器裡。
- 格線改由 `::before` 畫在繪圖區上，百分比自然就是刻度的位置
 （原本畫在整個容器上，每一條都偏在刻度中間，實測 B 差 15px）。
- 刻度帶自己的 `ratio`（＝該等級的柱高比例），畫面與匯出的 SVG
  **都**用 `1 - ratio` 定位——不是兩邊各算一次。

### 守門與反面測試

`e2e-chart-page.mjs` 第⑤組：

- ⑤ 每一根柱子的頂端都落在自己那一級的刻度上（**量畫面像素**，不讀原始碼推算）
- ⑤-2 匯出圖檔（同一份 chartModel 畫出來的 SVG）也是
- 兩條各自附一條前置（量到 0 根就會紅，不會恆真）

反面測試（把刻度改回平均分）：**❌ D 差 17.5px**，且 ⑤-2 的前置一起紅
（刻度標籤變成物件字串，對不到等級）——沒有讓它安靜地通過。

---

## 四、本輪跑過的守門

交通服務水準：`e2e-nav-collapse`、`e2e-chart-page`、`e2e-view-isolation`、
`e2e-nav-sections`、`e2e-nav-coverage`、`e2e-chart-layout`、
`e2e-figure-note-layout`、`e2e-nav-zone`、`e2e-nav-scroll-position`、
`e2e-text-wrap` ——全綠。

全日交通量：`e2e-nav-collapse` 全綠（含反面測試）。
路口轉向：`e2e-nav-collapse`、`e2e-nav-zone` 全綠（含反面測試）。

⚠️ 本輪依 2026-09-14 談好的流程，屬於「頻繁修改期間只跑必要檢查」，
**不是**完整大檢查。完整大檢查排在主工具列升級完成之後。

---

## 主工具列上線（三態：鏡子／脫離／回歸）

### 使用者原話

> 「主工具列＝彙整所有圖表工具列的篩選條件」
> 「圖可以自己改，但只影響那一張」
> 「針對共同的篩選條件……圖表自身的工具列仍舊要與主工具列同步」
> 「主工具列也多一個全部回歸鈕……避免有部分圖表忘記點選回歸」
> 「有圖表不適用的條件，該圖表就單獨顯示不適用」
> 「季度一律只做起訖區間，預設起＝迄」

### 這一支的條件

季度起訖區間（預設起＝迄＝最新一季）、路段多選、日別（平日／假日／並列）、
方向（方向1／方向2／並列）、尖峰（代表尖峰／上午／下午／並列）。

### 不可以弄錯的兩件事（都已寫成測試）

1. **先篩再挑最差。** 彙總的每一列是「同一組（計畫・年・季・路段・日別）
   四筆裡最差的那一筆」。主工具列篩了方向或尖峰時，必須**先把候選縮到符合條件
   的那幾筆、再挑最差**；拿已經建好的 `state.summaries` 去篩，只會剩下
   「剛好代表值就是方向1」的那幾列，其餘整列消失——使用者會以為那些路段
   那一季沒有資料。`main-filters.test.mjs` 用**手算**的期望值釘住
   （方向2 下午 LOS C、ratio 0.52）。
2. **預設條件下一個數字都不可以變。** 同一份明細、同一支排序函式、同一種分組鍵，
   `summariesFor()` 必須與升級前 `rebuild()` 的結果逐筆相同。

### 「並列」是呈現方式，不是篩選

平日＋假日、雙向、上午＋下午三個「並列」在資料層**一筆都不篩掉**；
把它當篩選的話，「全部」與「並列」就沒有差別了（已有測試）。

### 起＝迄的意思

起＝迄是「還沒動過區間」的預設狀態：一次只看一季的區塊取結束季度，
**歷季類的區塊（LOS 圖、歷季旅行速率、趨勢、三段分法）看全部季度**。
拉開之後才真的篩。這一條三支程式相同。

### 成果交付與結論草稿維持獨立

使用者明確裁示這兩頁不自動跟著主工具列跑（報告常常要輸出一份和畫面不同的範圍），
改為各加一顆「套用主工具列目前的條件」。`e2e-apply-main.mjs` **兩件一起驗**：
不驗「維持獨立」的話，一個「其實一直自動跟著跑」的實作也會全綠。

### 這一輪順帶抓到的兩件事

- **測資不像真資料**：`e2e-column-filter.mjs`／`e2e-reveal.mjs` 造的假明細只給
  `period`、沒有 `year`／`quarter`，而彙總的分組鍵用得到那兩欄，於是兩季被併成
  一組、彙總從 6 列縮成 3 列。已補上，並在原地寫明為什麼不可以省。
- **舊守門打在已經沒有人讀的欄位上**：`e2e-trend.mjs` 用 `trendState.day` 切日別，
  但日別已經升到主工具列。改成走這一塊自己的 `#trendDay` 下拉（＝使用者的操作）。

### 守門

`main-filters.js` ＋ `main-toolbar.js`（純資料、可單獨測）、`main-filters.test.mjs`（9 條）、
`e2e-main-toolbar.mjs`（三態，連數字一起量）、
`e2e-filter-coverage.mjs`（每個條件 × 每一頁：不是真的算了就是寫明不適用，
並反面守「回到預設之後不可以還留著任何一句不適用」）、`e2e-apply-main.mjs`。

---

## 五、旅行速率／行駛速率的標籤掃描（X-65）

### 使用者原話（2026-09-17）

> 「旅行速率和行駛速率是自動掃描文字，選擇對應欄位的數值判讀嗎?
>   例如以前的資料 A1 欄位是旅行速率、A2 是它的數值，B1 是行駛速率、B2 是它的數值，
>   如果哪天調查員把這兩項測值反過來放……程式可以判讀出來嗎?
>   另一種情況，調查員誤植文字 變成有 2 筆旅行速率或 2 筆行駛速率
>   系統可以判別出來，並提出異常警告嗎?」

### 查證結果

**一、是認標籤，不是認欄位位置。** `findLabels()` 逐格掃整張工作表找標籤文字，
`metricAt()` 才從**那個標籤自己**的位置取值（同格冒號後 → 往右最多 5 欄 → 往下），
往右時**碰到第一個非空文字格就停**（所以旅行速率那格是空的時候，
不會一路滑過去撿到行駛速率的數字）。因此把 A／B 兩欄整組對調，讀到的數值照樣
跟著各自的標籤走——已用「正常版面」與「對調版面」兩份矩陣比對，兩者結果完全相同。

**二、重複標籤的守門，原本只有一半。**

| 版面 | 兩筆「旅行速率」 | 兩筆「行駛速率」 |
|---|---|---|
| 有「旅次編號」分隔線（實際報告的主要版型） | ✅ 早就會報錯 | ✅ 早就會報錯（`single()`） |
| 沒有分隔線、退回舊解法 | ✅ 早就會報錯 | ❌ **靜靜挑最近的那一個** |

右下角那一格就是這次補的。舊解法對行駛速率用的是 `nearestMetric()`——
「離這個方向最近的那一個」——找到就用，不數有幾個。而且**物理檢核攔不到**：
兩個行駛速率只要都大於旅行速率、也都不等於它，`implausibleReason()` 一句話都不會說，
`issue` 是空的、`ok=true`，數值就這樣寫進資料庫。

修法：舊解法也照方向的區塊界線數「平均總行駛速率」，超過一個就整張表報錯——
和有分隔線那條路的 `single()` 同一條標準：

> 「上午尖峰」工作表中方向1有 2 個「平均總行駛速率」，系統不會自行挑選或平均，請確認報告只保留一個代表值

**三、誤植成「2 筆行駛速率、0 筆旅行速率」也接得住**：那一趟讀不到旅行速率，
會落到「切出 N 趟旅次，但只有 M 趟讀得到『平均總旅行速率』」這一句。

**四、系統一律不自行對調。** 物理檢核（旅行速率不得大於行駛速率；有延滯時兩者不得相等）
只負責**指出**欄位可能錯位，畫面上明寫「⚠️ 系統不會自行把兩個值對調——那等於替調查資料做決定」。

### 守門

`speed-label-scan.test.mjs`（4 條）：對調後結果不變、兩筆旅行速率要報錯、
兩筆行駛速率要報錯、有旅次編號的版型同樣要報錯。

**反證（實跑）**：把新加的計數整段拿掉，只有「兩筆行駛速率」那一條變紅
（`expected 0 to equal 2`），其餘三條仍綠——確定不是恆真。
第三條的假資料刻意讓兩個行駛速率**都符合物理常識**，否則紅的會是物理檢核、
不是這條守門。

`node parse-harness.mjs test-fixtures` 對 6 份真實測試檔重跑，全部「讀取完整」，
數值與修改前逐筆相同。
