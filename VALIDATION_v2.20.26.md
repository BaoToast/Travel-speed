# 交通服務水準 v2.20.26 驗證報告

- 驗證日期：2026-09-01
- 發布目標：`BaoToast/Travel-speed` GitHub Pages
- 前一正式版：v2.20.24（commit `c2c296b6f53ef1edcc1fe4a602d0d3badf143bb5`）
- 複查來源：使用者提供的 v2.20.25 候選包（SHA256 `F992FA3D9794BCD23673F4BEE8BA78342E1CAC299080858D99A20129FB9A7BBD`）

## 本版修正

### 1. 獨立交付包的測試依賴

v2.20.25 新增的季度把關測試直接匯入壓縮包外的 `../tt143/lib/period-date.ts`。在全新資料夾解壓後執行完整測試，結果為 110 項中 1 項失敗，錯誤為 `ERR_MODULE_NOT_FOUND`。這代表候選包不能獨立驗證。

v2.20.26 改以交付包內的行為規格表驗證民國 90～200、西元 2001～2111、四個季度，以及格式錯誤與超出範圍案例，不再依賴其他系統的工作目錄。

### 2. 版本文件一致性

v2.20.25 候選包的 README 首章仍為 v2.20.24，驗證報告檔名雖為 v2.20.25，但標題、結論、測試數與手冊說明仍記載 v2.20.24；更新說明亦把上一版排序修正誤標成 v2.20.25。

v2.20.26 已同步 README、更新說明、驗證報告、網站、快取參數及手冊，並在 `check-version.mjs` 新增 README 第一版本章節、驗證報告檔名與報告標題的自動守門。

### 3. Claude v2.20.25 的季度共用函式

`period-date.js` 新增 `checkSurveyPeriodInput()` 與 `surveyPeriodInputMessage()`，用來統一三支系統的季度輸入規格。交通服務水準既有尖峰匯入流程原本已限制民國 90～200／西元 2001～2111，本次同步不改變既有寫入行為或資料格式。

複查另發現候選函式先正規化再用 2～3 碼外形判斷，會把 `89Q1` 與 `201Q4` 誤判為合法。v2.20.26 改為先依輸入年碼長度區分民國／西元，再明確套用各自範圍。這項修正只作用於新共用函式，既有尖峰匯入主流程本來就有正確範圍檢查。

## 計算規則界線

本版沒有變更：

- 旅行速率、行駛速率及延滯解析或計算；
- LOS 門檻：A ≥ 0.80、B ≥ 0.60、C ≥ 0.50、D ≥ 0.40、E ≥ 0.20、F < 0.20；
- 代表紀錄順序：先取最差 LOS，再取最低速限比，再取最低旅行速率；
- 旅行速率、行駛速率及總延滯必須來自同一筆代表紀錄；
- 各計畫資料與自訂 LOS 門檻彼此隔離。

## 驗證結果

- 單元／一致性測試：111 項，109 通過、0 失敗、2 略過。兩項略過均明確依賴本次附件未附的真實調查檔。
- 六組瀏覽器操作測試全部通過，涵蓋匯入與 Excel、結論草稿、方向名稱與 Manager、15 個分頁及 9 種寬度、日期判讀、民國／西元顯示切換；全程無 JavaScript 例外。
- 版號、快取參數、手冊檔名、封面戳記、README、更新說明及驗證報告一致性檢查全部通過。
- PDF 手冊 35 頁、Word 手冊 25 頁；PDF 直接轉圖，Word 因環境沒有 LibreOffice，改由 Microsoft Word 輸出 PDF 後逐頁檢查。未見截斷、重疊、缺字或破版。
- Claude 說明中的 37 份真實檔解析比對屬交接紀錄；原始調查檔未包含在附件內，因此不列為本次獨立複驗結果。

GitHub Actions、Pages 版本、線上程式與手冊雜湊，以及舊版網址 404 會在推送後完成查核。
